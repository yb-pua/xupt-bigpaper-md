# 方向设计汇总版（论文写作事实源）

> 单一事实源（2026-08-17 汇总）。吸收原《方向一设计-C1C6》《方向二设计-D0D6》《三方向设计与论文组织》。
> 论文写作直接引用本文件，不再引用任何分方向文档。
> 配套：需求侧《实验需求点清单.md》、专利侧《专利主成果对照-八方向增删改.md》、代码侧《代码汇总版.md》。
> 铁律：数值对比全部同环境自实现；文献仅方案参照（模态/数据集/提取器差异显式声明）；呈现用表格/柱状图/曲线，不用圆形图；先立主张再设计对比；攻击用例三方向统一编号（A/B/C 前缀）。

---

## 1. 三方向链路与共享底座

```
方向一（用户身份层）          方向二（设备组网层）          方向三（应用权限层）
生物特征→bio_key→生物DID ──→  设备标识→设备DID ──────────→  双DID: 用户生物DID + Agent设备DID
KGC-TEE 派生 SM9 私钥          KDC 签发 ST(NetPerm)          KDC 签发数据/协同权限 ST
AS-REQ 验签认证 ──────────→    中继代理签名+三重验证 ──────→  MCP 验证 签名链 + 双 ST
（用户认证入口）               （组网准入）                  （协同权限管控）
```

共享底座（方向一实现、二/三复用）：`did.py`（生物+设备 DID）/ `sm9_engine.py`（签名、验签、密钥协商、私钥派生、verify_chain、proxy_*）/ `kdc_tee.py`（KDC 单实例）/ `ticket.py`（ST 签发验证，claims 扩展）/ `binding_table.py`（用户↔设备绑定）/ `audit_logger.py`（ticket_id 审计链）。

---

## 2. 论文组织

```
第 3 章 = 方向一（四段式：方案设计→实验→对比→小结）——见 §3
第 4 章 = 方向二（同四段式）——见 §4
第 5 章 = 方向三（同四段式）——见 §5
第 6 章 = 综合对比：共享底座复用、攻击矩阵 A/B/C 总表（统一用例编号）、
         数据流转全景（ticket_id 贯穿）、三方向联动场景
```

写作铁律：①数值对比同环境自实现；②先立主张再设计对比；③攻击用例三方向统一编号。

### 总体叙事主轴（三方向衔接）

**一句话**：一个人的身份，经过一次认证，管住一台设备、放行一类服务、贯通一条审计链。

```
身份怎么认证（你是谁）→ 通道怎么建（设备怎么入网通信）→ 服务怎么用（Agent 怎么调 MCP）
       方向一（第3章）             方向二（第4章）                    方向三（第5章）
  生物特征→生物密钥            设备DID+ST票据组网              双DID+双ST+双签名链
  →SM9身份→Kerberos认证        →P2PVPN中继代理签名            →MCP四步验证分级授权
```

每章用同一"四段式"叙事骨架，后层复用前层已建立的"身份+票据"事实，形成**可叠加的信任递进**：

1. **问题链（威胁模型）**：现状安全缺陷 → 具象为攻击面（A5/B4/C3 用例编号预埋）；
2. **方案链（机制）**：本方案如何逐条堵住问题链（机制↔用例一一对应）；
3. **数据支撑（实验）**：同环境自实现数值 + 内部参数消融（见 §6 对比参数总表）；
4. **对比与创新（落点）**：组合空白定位 + 文献方案定性 + 论文位置。

**信任递进关系**（每章结尾一句）：方向一解决"凭证是谁的"，方向二解决"凭证从哪来"（设备通道），方向三解决"凭证能干什么"（服务授权）——三者用同一票据体系（ST/双 ST）+ 同一条审计链（ticket_id）焊成整体。第 6 章用一个三方向联动场景收束（用户→设备→Agent→MCP 一次完整调用，ticket_id 全程可查）。

---

## 3. 方向一设计（C1-C6）

### 开题主张（方向一）

① 模糊提取生成稳定生物密钥（多数投票+码辅助数据，含噪可靠恢复）；② 用户ID+生物密钥→生物DID，KGC-TEE 派生 SM9 私钥；③ 重构 AS-REQ（"ID+DID+时间戳"SM9 签名替代口令），阻断票据伪造与重放。

### C1 生物密钥可恢复（实验 A1）

- **实验**：LFW 自有数据算成功率。主指标 KRR/FAR_key/FRR_key + Hamming 距离接受阈值 θ 扫描出 AUC/EER；**投票消融曲线（2026-08-19 定稿）：登记照片数 k=1..5 为消融变量，统一取 ≥5 张的 423 人队列——登记前 k 张投票生成 σ、验证第 5 张真实跨条件图（k=5 档验证张与登记重叠=同源上限），同源曲线（验证同人其他图）对照**。数据口径见《代码汇总版》§3.1。
- **实测（2026-08-18 缓存重建后）**：single KRR=0.287 / vote5 KRR=0.986（+69.9pp，BER 29.4→15.3 落入 RS t=32 容限）；FAR=0（5000 异人对）；EER=0.027 / AUC=0.989；跨条件单张=0.313（BER 均值 29.2、p95≈32 触及 RS 容限边缘——**系单张对单张口径，非方案使用方式**；登记侧投票后 BER 预期 ~15 落入容限）。
- **补跑（P0）**：k=2,3,4 档消融 + 跨条件投票曲线（改 expA1_biometric_key.py 的 A1-3：cross 队列改为 423 人、登记前 k 张投票）；论文呈现"KRR vs 登记照片数"双曲线（跨条件/同源），论证"多数投票"机制与"采集 10 张、阈值 0.8"参数。
- **对比向**：无文献数值对比（Kuznetsov 2023 / Yirga 2025 / CCS2025 / Sulavko 2025 仅引言引用）。
- **论文位置**：3.3（方案）+ 3.7（实验）。

### C2 噪声鲁棒（实验 A2）

- **实验**：图片级加噪→重新恢复（用户口径）：cv2 五类扰动（高斯/亮度/旋转/模糊/遮挡）×强度梯度×KRR；注册侧加噪生成、认证侧加噪恢复；LFW 跨条件（A/B 图）等效噪声锚点标注在曲线，论证 RS(255,191,t=32) 容限覆盖真实噪声。
- **对比向**：无数值对比（Becker 2013 仅理论引用）。
- **论文位置**：3.3（方案）+ 3.7（实验）。

### C3 认证基线对比（数学理论复杂度矩阵，工程实测放弃）

- **决策（2026-08-19）**：SM9 vs RSA/ECDSA 跨实现实测不可比（gmalg 纯 Python vs cryptography C 库，实测差距 10^4 中数学复杂度仅占 10^2-10^3，其余为实现差距）——**放弃 AS-REQ 三基线工程组装与 RSA-PKINIT 实现，改为安全强度归一的理论复杂度对比**；原语时延仅作工程参考（标注实现库/语言，不跨实现对比）。
- **免证书方案谱系（选型论证）**：SM9=国密标准化 IBS（GMT 0044）；同族=经典 IBS（Cha-Cheon 2003 / Hess 2002，均依赖双线性对）；无配对变体=Shamir 1984 IBS（RSA 假设，无标准化）/ 格基 IBS（SIS/LWE，密钥与签名 KB 级）/ Cocks IBE（密文膨胀）/ 自认证 Girault 1991（部分免证书）；对照=PKI-RSA / PKI-SM2（证书链）。
- **理论对比矩阵（T1..Tn，128bit 安全级归一，实现无关）**：

| 维度 | PKI-RSA-2048 | PKI-SM2 | 经典 IBS（配对） | 无配对 IBS（格基） | **SM9（本文）** |
|---|---|---|---|---|---|
| 免证书（身份即公钥） | ✗（证书链） | ✗ | ✓ | ✓ | ✓ |
| 配对依赖 | ✗ | ✗ | ✓ | ✗ | ✓ |
| 安全假设 | 大整数分解（112bit） | ECDLP（128bit） | BDH | SIS/LWE | BDH（128bit） |
| 验签复杂度（等效模乘） | ~34（e=65537 小指数） | ~7.7k（2 次标量乘） | ~20-30k（1 双线性对） | 中（参数大） | ~20-30k（1 对+2 标量乘） |
| 签名复杂度（等效模乘） | ~3k（2048 位模幂，慢） | ~3.8k（1 次标量乘） | 与 SM2 同量级 | — | 与 SM2 同量级（可预计算） |
| 签名/密钥尺寸 | 256B/256B | 64B/32B | 64B/32B | KB 级 | 64B/32B |
| 国密标准化 | ✗ | ✓（SM2） | ✗ | ✗ | **✓（GMT 0044）** |

- **结论句**："以 128bit 安全级统一度量，SM9 验签数学复杂度约为 SM2 的 2-4 倍（双线性对主导）、RSA-2048 验签的 10^2-10^3 倍（后者依赖小指数 e=65537 结构，且其签名方向反而慢一个数量级）；相比其他免证书 IBS 变体，SM9 具备国密标准化与工程生态优势——计算代价换取免证书与国密合规，属可接受权衡。"
- **叙事**：安全价值在前（C4 拦截率 9/9 vs 口令版）→ 代价在后（数学复杂度表 + 实测参考表）。
- **论文位置**：3.7（实验）。

### C4 攻击面（实验 A5）

| 攻击手段 | 攻击原理 | 本文防御机制 | 验证 |
|---|---|---|---|
| 黄金票据（Golden Ticket） | 窃取 KDC 主密钥→Mimikatz 伪造任意 TGT | KDC 主密钥 TEE 中生成/使用、不出 TEE | A3 审计 |
| 白银票据（Silver Ticket） | 窃取服务密钥→伪造任意 ST | 服务密钥 KDC-TEE 派生；服务访问前置 SM9 验签 | A5-2 |
| 重放 | 捕获请求/票据重放 | 时间戳 30min + nonce | A5-1 |
| 离线破解/Kerberoasting | 离线爆破服务账户口令 | 无口令；票据 SM4+密钥 TEE 管理 | A5-5 |
| 口令喷射 | 批量弱口令尝试 | 无口令+熔断（3 次锁定） | A5-5 |
| 票据传递（Pass-the-Ticket） | 窃取内存票据异地使用 | 票据绑定 DID+SM9 签名身份 | A5-3 |
| AS-REP Roasting | 未预认证账户离线破解 | 全部账户强制预认证 | A5-2 |
| 委派滥用 | 委派配置错误提权 | 不引入委派；代理签名受 NetPerm 限定 | B 系列 |
| 时间同步放宽 | 窗口过宽放大重放窗口 | 30min+nonce 双保险 | A5-1 |

- **对比向**：同用例集自实现拦截率（本文 vs 口令版）；Qatinah 2024 攻击面覆盖度声明。
- **论文位置**：3.6（安全分析）+ 3.7（实验）。

### C5 隐私论证（传统人脸认证 vs 本文）

4 步论证链（佐证文献）：
1. **传统存模板、模板可逆向重建**：Mai TPAMI 2019（41(5):1188-1202）；Shahreza ICCV 2023（3D 重建）；Gomez-Barrero & Galbally, Comput. Secur. 90, 2020（逆向综述）；**TIFS 2021**（攻击二值化+模糊承诺人脸系统，>83% 解锁 @FMR 0.1%——与本文链路同构，必引）；
2. **本文不存模板（σ+key_hash）**：ISO/IEC 24745:2022；Dodis 2004；
3. **生物特征不可撤销，本文可重注册**：ISO 24745 Renewability；Rathgeb & Uhl 2011；Nandakumar & Jain SPM 2015；
4. **存储变换模板也不安全**：Lacharme SECRYPT 2013（BioHashing 预映像攻击）；Shahreza FG 2024（Breaking Template Protection）。

对比表（论文 3.1 结尾）：存储内容（模板 vs σ+key_hash）/ 特征是否出本地 / 泄露后果（身份冒用 vs 无法恢复，A5-4 实测≈0）/ 可撤销性 / 认证凭据（静态模板 vs 动态密钥）。

- **论文位置**：**3.1 威胁模型与设计目标（动机：传统隐私缺陷→设计目标）+ 3.6（ISO 24745 对照+A5-4 验证）**——不放最后。
- 总结句："传统服务以'存储模板'换认证，泄露即身份终结；本方案'不存模板+特征不出本地+辅助数据不可逆'，泄露不致命、可重注册。"

### C6 认证判定链与熔断恢复

- **判定链**："认证判定 = Rep 恢复 bio_key' ∧ SM3(bio_key') == 登记 key_hash；RS 超纠错可能误纠错到错误码字，哈希校验兜底；σ 篡改仅致拒绝服务而非伪造通过。"
- **熔断分层恢复**：L1 会话重建（熔断 3 次→删会话/票据→Rep 恢复→重认证，bio_key/DID/授权不变）；L2 身份重置（Rep 连续失败→重新 Gen→显式更新 key_hash/DID/私钥→重新授权，低频事件）。
- **实验**：A3 异常路径（熔断→L1→断言 bio_key/DID/票据链不变）；A5-5（熔断触发率、L1 成功率、L2 兜底率——L2 应极低，与 A2 KRR 交叉印证）。
- **论文位置**：3.6（安全分析）。

### 方向一数据契约（跨方向预留）

- Ticket 含 `ticket_id`（流转关联键）；`verify_ticket(ticket, service, claims_checker=None)`（方向二 NetPerm / 方向三权限集合）；
- sm9_engine 预留：`verify_chain(sigs, docs, dids)`（方向三签名链）、`key_exchange_*`（方向二/三密钥协商）、`proxy_sign/proxy_verify`（方向二授权委托）；
- `audit_logger`：统一日志（ts, ticket_id, action, result, principal）——KDC/中继/MCP 全链路可回溯；
- 票据存储：客户端缓存（ccache）+ KDC 无状态；撤销靠短 TTL（30min）+熔断+重签。

### 方向一文献（2026-08-19 全部核对完成）

- **TIFS 2021（二值化逆向，C5 核心）**：Keller D, Osadchy M, Dunkelman O. Inverting Binarizations of Facial Templates Produced by Deep Learning (and Its Implications). IEEE TIFS, **16:4184-4196, 2021**. DOI 10.1109/TIFS.2021.3103056。关键数据：**>83% 重构模板在 0.1% FMR 下解锁账号**；跨系统攻击成功率高出 FMR 170–210 倍；攻击对象=二值化+模糊承诺，**与本文链路同构，必引**。
- **Mai TPAMI 2019**：Mai G, Cao K, Yuen P C, Jain A K. On the Reconstruction of Face Images from Deep Face Templates. TPAMI, **41(5):1188-1202, 2019**. DOI 10.1109/TPAMI.2018.2827389。type-I 攻击 TAR=**95.20%** @ FAR=0.1%（LFW）。
- **Shahreza ICCV 2023**：Shahreza H O, Marcel S. Template Inversion Attack against Face Recognition Systems using 3D Face Reconstruction (GaFaR). **ICCV 2023, pp.19662-19672**。
- **Shahreza FG 2024**：Shahreza H O, Marcel S. Breaking Template Protection: Reconstruction of Face Images from Protected Facial Templates. **FG 2024, pp.1-7**——保护后模板仍可重构，支撑"非存储式 σ"设计必要性。
- **Shahreza 基准（综述标的 JIVP 2026）**：Benchmarking of Cancelable Biometrics for Deep Templates. **J. Image Video Process. (EURASIP), 2026**（arXiv:2302.13286）——exp5/6 同类评测（BioHashing/MLP-hash/Bloom/IoM）。
- **Gomez-Barrero & Galbally, Comput. Secur. 90, 2020**（逆向综述）——已入文献库。
- 方案参照：Kuznetsov 2023 / Yirga 2025 / CCS2025 / Sulavko 2025（C1）；Becker 2013（C2）；Qatinah 2024（C4）；ISO/IEC 24745:2022、Dodis 2004、Rathgeb & Uhl 2011、Nandakumar & Jain SPM 2015、Lacharme 2013（C5）。

---

## 4. 方向二设计（D0-D6）

### 开题主张（方向二）

设备 DID 关联 SM9 私钥 → KDC 对"设备DID+访问策略"生成 SM9 授权并委托 P2P 中继节点为代理签名者 → P2P 网络分布式组网实现节点发现与拓扑构建 → 代理签名者验证"SM9授权有效性+ST票据合法性+设备DID一致性"，为合法客户端提供通信中继 → SM9 密钥协商生成会话密钥+带特征流量整形安全隧道 → 实现组网准入控制与端到端通信安全结合。

### 关键决策（2026-08-17 拍板）

1. **中继=验证型网关**：中继验证 ST/授权/签名链后按 NetPerm 路由转发，**不碰明文**——保留"中继不可解密"主张（不推翻 D5），与方向三 MCP 兼容方案 B+C 复用。
2. **节点发现/拓扑构建=简化方案**：协调服务（controller 兼任）做设备注册/发现（DHT 简化替代）+ 中继邻居表构建星型拓扑；**仅证明可行**，不追求完整 DHT/NAT 穿越。
3. **特征流量整形必做**（开题承诺"带特征流量整形构建安全隧道"），B3 不降级。
4. **指标分层**：连通率/丢包率等为**性能面**参数（功能正确性+代价），与**安全面**参数（拦截率/不可解密/授权粒度）分开呈现。
5. **部署分两阶段**：阶段一本机多进程源码跑通（coordinator 用 subprocess 编排 kdc/relay/device）；阶段二三方向完成后 Docker+Web 联调（2026-08-17 决策）。
6. **代理签名保留（warrant 限定）**：中继真实签名任务=会话准入凭证签发（授权书限定范围，不能签新授权）；伪造边界=授权书范围。配合 NAT 穿越条件量化（D0）证明中继=真实必需基础设施（NAT 兜底），非人为引入。

### 开题对照（6 要素全部闭合）

| # | 开题原文 | 对应设计/实验 | 状态 |
|---|---|---|---|
| 1 | 设备 DID 关联 SM9 私钥 | device.py + KGC 派生（复用方向一 sm9_engine/did/kdc_tee） | ✓ |
| 2 | KDC 对"设备DID+访问策略"生成 SM9 授权，委托中继为代理签名者 | 授权 A={DID_dev, policy, exp, Sig} + 代理密钥委托 | ✓ D2 |
| 3 | P2P 网络分布式组网实现节点发现与拓扑构建 | 协调服务注册/发现 + 中继邻居表星型拓扑 | ✓ D0（简化） |
| 4 | 代理签名者验证授权/票据/DID 一致性，为合法客户端提供中继 | 三重验证 | ✓ D1 |
| 5 | SM9 密钥协商会话密钥 + 带特征流量整形安全隧道 | key_exchange + 整形 | ✓ D5/D6 |
| 6 | 组网准入控制与端到端通信安全结合 | B1（准入）+ D5（端到端） | ✓ |

### D0 组网基础设施（节点发现+拓扑构建）

- **实验**：协调服务设备注册/公告 + find_node 查询（DHT 简化替代）；中继维护邻居表构建星型拓扑；新设备"发现→入网→与已准入设备通信"全链路可行性证明。
- **NAT 穿越条件量化（中继必要性论证）**：①理论矩阵——NAT 类型对（全锥/受限锥/端口受限/对称）× 直连可行性，引 RFC 4787/5389/5766；②模拟打洞——设备加虚拟 NAT 层（类型可配），代表性类型对实测直连成功率；③兜底率推导——假设 NAT 类型分布，计算需中继兜底比例，并对比"直连 vs 走中继"时延/吞吐差。输出 `expB1_nat.csv`。论据链：**P2PVPN 无法无条件打洞 → 中继=真实必需基础设施（NAT 兜底）→ 代理委托（D2）与数据面兜底（D4）有理**。
- **简化声明（论文口径）**：以集中信令替代 DHT 实现节点发现，拓扑为星型；证明"分布式组网技术可支撑票据化准入"即可；NAT 穿越以理论矩阵+代表性模拟呈现（单机环境，对称 NAT 等难点如实标注）。
- **对比向**：内部机制验证（可行性）+ 方案引文（[17] TUN P2PVPN、[22] 国密 P2P VPN 的 DHT/信令参照）。
- **论文位置**：4.2（组网与拓扑）+ 4.5（实验）。

### D1 票据化准入有效拦截（实验 B1+B4）

- **实验**：6 类节点（合法/过期/重放/伪造/篡改/冒用）分类正确率（应 100%）；B4 七用例拦截率；与"无票据 P2PVPN"基线同用例拦截率对比（安全差距证据）。
- **联动用例（方向一）**：B1 新增"绑定校验"——设备入网须关联已认证用户（绑定表 user_bio_did↔device_did）；无绑定/绑定用户未认证 → 拒绝。"先认证用户，再授权设备"零信任联动。
- **对比向**：数值自实现（同环境无票据基线）；方案引文（[22][51] 现状参照）。
- **论文位置**：4.3（方案）+ 4.5（实验）+ 第6章安全属性矩阵。

### D2 授权委托安全（代理签名，warrant 限定）

- **机制**：KDC 生成授权 A={DID_dev, policy, exp, Sig} + 代理签名密钥分发中继，附**授权书（warrant）**限定"只能为通过三重验证的设备签发**会话准入凭证**（绑定 DID+NetPerm+有效期），不能签发新授权/新票据"；中继代理验签，并为准入设备签发会话准入凭证（数据面凭此快速放行，避免每包重验授权）。
- **实验**：中继代理验签；**伪造能力测试**——授权书范围外签发凭证 100% 无效（中继无 KDC 主密钥，无法伪造新授权/越权授权）。
- **对比向**：内部机制验证（伪造测试）；方案引文（SM9 高级原语 [12][13][14][15]；SM9 代理签名[见下]）。
- **论文位置**：4.3（方案）+ 4.5（实验）。

### D3 票据化开销（实验 B1 时延 + B2）

- **实验**：准入时延三段分解（授权验签/ST 校验/挑战应答，p50/p90）+ vs 无票据基线增量；随 N 扩展。
- **对比向**：数值自实现（同环境无票据基线）。
- **论文位置**：4.5（实验，Q2）。

### D4 去中心化增益（实验 B2）

- **实验**：N=10/50/100/200 扩展性曲线（准入时延/聚合吞吐/e2e 时延/丢包）；vs OpenVPN 网关同环境；**故障注入**（kill 中继 vs kill 网关 → 中断范围/恢复时间）。
- **对比向**：数值自实现（同环境 OpenVPN）。
- **论文位置**：4.5（实验，Q3）+ 第6章综合。

### D5 SM9 密钥协商隧道（实验握手/吞吐 + B4-4）

- **实验**：SM9 密钥协商握手轮次/时延、隧道吞吐、中继侧仅见密文（B4-4 恶意中继）；vs **TLS 隧道基线（python ssl 自实现，同环境）**；IPsec 全栈自实现不现实，以文章定性+文献数值引用（IPsec 网关终止可解密 vs 本文中继不可解密）。
- **对比向**：数值自实现（TLS 自实现基线）+ 方案引文（中继不可解密 vs TLS/IPsec 网关终止，定性）。
- **论文位置**：4.4（方案）+ 4.5（实验）。

### D6 特征流量整形（实验 B3）

- **威胁模型（论文立论，2026-08-19 核对）**：流量进 SM4 隧道后应用层内容（HTTP/HTTPS/DNS 的 URL/SNI/证书/域名）已加密，观测者**看不到协议字段**；但能看到**统计特征**（包长分布/包间隔 IAT/突发结构/方向/上下行比/会话时长），可被**网站指纹识别（WF）**利用——Wang et al. 2014（k-NN，4226 特征）、Sirinam et al. 2018（Deep Fingerprinting，深度学习击穿防御）、CACM 2024 OpenVPN 指纹（"均匀加密包稳定流"是 VPN 死穴 vs web 不规则突发）。**整形对象 = 统计痕迹，非协议字节**。
- **实验**：三型业务流（周期/突发/交互，**输入建议用真实协议模式**：HTTP 请求-响应、HTTPS 的 TCP+TLS 突发、DNS 小包高频——比抽象型更真实）整形前后包长/间隔分布、分布熵 H、与均匀参考分布 KL 散度、带宽冗余率（≤15%）、时延增量。防御形态对齐学术同类（WTF-PAD/Walkie-Talkie 的填充+缓冲）。
- **范围声明（论文）**：DNS 隧道建立前的 DNS 关联攻击（DNS correlation）为独立面，作范围声明或补充；不涉 Tier3 协议模仿（伪装 HTTPS，超出本文）。
- **对比向**：内部参数（整形前后自对比）；方案引文（[29] 隐蔽通信、[32] 匿名追踪、WF 文献 Wang2014/Sirinam2018/OpenVPN指纹、Tor/pluggable transports 待补库）。
- **论文位置**：4.4（方案）+ 4.5（实验，Q4）。

### 学术领域对比表

| 主张 | 领域 | 对方代表工作（库内） | 对方做法 | 我的差异化 | 论文定位 |
|---|---|---|---|---|---|
| D0 | P2P 组网/节点发现 | [17] TUN P2PVPN、[22] 国密 P2P VPN（DHT/信令待补） | DHT 节点发现成熟（Kademlia/libp2p），重完整组网 | 简化信令替代 DHT，仅证可行；集中管控+分布转发 | 4.2 组网 |
| D1 | 零信任设备身份/Kerberos-IoT | [20][51][2][3] | 证书/口令/设备指纹认证；Kerberos-IoT 重轻量化 | 设备DID+ST(NetPerm)+三重验证+用户-设备绑定联动 | 4.3 方案 + 4.5 实验 |
| D2 | SM9 高级原语/代理签名 | [12][13][14][15] | 算法方案设计为主，工程化中继部署少 | 代理签名落地中继，伪造测试证明委托安全 | 4.3 方案 |
| D3 | Kerberos 性能工程 | [21][3][4] | 算法级认证时延对比 | 组网准入时延三段分解+增量量化 | 4.5 实验（Q2） |
| D4 | P2PVPN vs 中心化 VPN | [17][22][7]；OpenVPN/WireGuard | 功能/拓扑设计为主，性能自比 | 同环境 OpenVPN 扩展性曲线+故障注入 | 4.5 实验（Q3）+第6章 |
| D5 | SM9 密钥协商/VPN 传输 | [33][40]；IPsec(RFC4301)/TLS(RFC8446) | SM9 多为对运算时延；IPsec/TLS 非国密、网关可解密 | 中继不可解密+全国密+同环境对比 | 4.4 方案 + 4.5 实验 |
| D6 | 流量分析防御 | [29][32] | Tor/隐蔽信道成熟，熵/KL 量化 | 整形嵌入 SM4 隧道，隐藏+带宽代价双量化 | 4.4 方案 + 4.5 实验（Q4） |

### 对比分类原则（三方向沿用）

- **数值自实现（同环境）**：D1（无票据基线）、D3（无票据基线）、D4（OpenVPN）、D5（TLS 隧道自实现；IPsec 定性）
- **方案引文（文章定性）**：D1（安全属性矩阵）、D2（SM9 原语）、D5（中继不可解密）、D6（整形路线）
- **内部参数（自对比）**：D0（可行性）、D2（伪造测试）、D6（整形前后）

### 性能面与安全面指标分层

| 面 | 指标 | 用途 |
|---|---|---|
| 安全 | 拦截率、中继不可解密、授权粒度、撤销时延 | 安全主张验证 |
| 性能 | 准入时延、吞吐、e2e 时延、丢包率、连通率 | 功能正确性 + 整形代价 + 去中心化对比 |

### 方向二文献（2026-08-19 核对更新）

- **SM9 代理签名（D2，已确认存在，非空白）**：
  - *A proxy-protected proxy signature based on SM9*. IEEE Xplore 9750508（会议）——SM9 代理签名构造。
  - 张雪锋, 王柯航. 一种基于SM9的代理环签名方案. 信息网络安全, 2025(12)（网络首发 2025-12-10）——代理委托+不可伪造性证明。
  - 朱郭诚, 何德彪, 安浩杨, 彭聪. 基于区块链和SM9数字签名的代理投票方案. 信息网络安全, 2024, 24(1):36-47——SM9 代理工程应用佐证。
- SM9 密钥协商标准/文献（GB/T 38635.3 或 SM9-KA 分析）——D5 依据
- P2PVPN/NAT traversal 学术代表（N2N/Wormhole/libp2p/Kademlia）——D0/D4
- VPN 性能基准（OpenVPN vs WireGuard vs IPsec 对比研究）——D4/D5 基线依据
- 流量分析防御（Tor pluggable transports、网站指纹、TrafficSliver 等）——D6

---

## 5. 方向三设计（C0-C4）

### 叙事定位（论文第 5 章主线）

MCP 安全"从无到有"：**无认证**（一手测量：**7,973 个远程 MCP 服务器中 40.55% 完全无认证**，Zhou et al. arXiv:2605.22333, 2026）→ **OAuth 2.1**（授权态复用/粒度粗/撤销慢等隐患）→ **本文 SM9 票据化**。MCPS（IETF draft-sharif-mcps-secure-mcp-00, 2026-03）/SMCP（arXiv:2602.01129）为同阶段密码学化共识，作三角佐证，定位"**互补非替代**"，不与首创之争纠缠。

**外网化论证（需求真实性六因）**：MCP 从内网 stdio 出身 → 远程化强制（OAuth 2025-03）→ 外网化必然——①网络效应 ②Agent 长尾能力 ③组织边界外数据 ④标准化降本 ⑤商业变现 ⑥用户即跨网 Agent。六因支撑"MCP 需要票据化安全"的需求真实。

### 开题承诺（逐条闭合）

① Agent 视为特殊设备节点，生成设备 DID 并关联 SM9 私钥；② KDC 签发内嵌组网权限的 ST 票据（ST_net），P2P 中继验证 SM9 授权及 ST 票据合法性后放行接入 P2PVPN；③ 用户生物 DID 关联 SM9 私钥，KDC 签发绑定数据/协同权限的 ST 票据（ST_data），MCP 服务端验证票据及权限匹配性；④ Agent 协同指令附加"用户生物DID签名 + Agent设备DID签名"SM9 签名链，携带双 ST；⑤ MCP 服务端校验签名链与 ST 有效性后转发，实现组网、数据、协同权限精准管控。

开题全文本对照：**14 个承诺点全部闭合（无缺口）**；2 处措辞对齐（Agent=特殊设备节点、中继验证写全"SM9 授权+ST+DID"）。

### Agent 身份锚点（"Agent=特殊设备节点"工程论证）

四类运行环境唯一标识：docker container id（64 hex）/ linux `/etc/machine-id`（32 hex）/ windows MachineGuid（36 字符）/ vmware UUID（36 字符）→ `make_device_did` 派生设备 DID。环境重建/克隆由"重注册+登记表"兜住。

### C0 Agent 身份锚点可行性

- **实验**：生成 4 类环境唯一标识 → `make_device_did` 派生 Agent 设备 DID → 唯一性/冲突重试；环境重建→新标识→重注册走登记表；克隆→相同标识→注册去重。
- **对比向**：内部机制验证（可行性）；把"Agent=特殊设备节点"从假设变成工程事实。
- **论文位置**：5.2（方案）论证 + 5.5（实验）。

### C1 分级授权功能（三场景）

- **实验**：场景 A 数据查询（用户授权 Agent 查库，ST_data 四步验证）、场景 B 协同任务（多 Agent 链式，每级双签名链+双 ST）、场景 C 跨域转发（经 mcp_gateway 验证 ST_net 访问异网 MCP）；审计 ticket_id 关联链贯通。
- **对比向**：内部功能验证（三场景跑通）。
- **论文位置**：5.3（方案）+ 5.5（实验）。

### C2 签名链性能（本文 vs OAuth 基线）

- **实验**：并发 100/500/1000 req/s；单请求验证时延 p50/p90/p99、QPS、消息尺寸开销；OAuth 基线同环境自实现。
- **对比向**：数值自实现（同环境 OAuth 2.1 授权码+PKCE）。
- **论文位置**：5.5（实验）。

### C3 指令攻击与正确性（三方案对比 + 消融）

- **实验**：≥500 条指令（正常 60%/篡改 16%/越权 16%/重放 8%），三方案（无管控/OAuth/本文）各跑一遍；C3-1~C3-6 攻击用例（见《代码汇总版》§5.4）；消融三档（全量/去用户签名层/去 ST_data）。
- **C3-6 调用者混淆**：忠实复刻 OAuth 现状"一次性授权后 server 级授权态缓存复用"缺陷，让攻击真打穿 OAuth 基线；本文每请求带认证器拦截。
- **对比向**：数值自实现（同环境 OAuth/无管控基线）+ MCPS/SMCP 佐证。
- **论文位置**：5.4（安全分析）+ 5.5（实验）。

### C4 安全属性对比矩阵

- **实验**：7 维（抗重放、抗伪造、权限粒度 tool 级、调用级绑定防调用者混淆、审计归因、撤销/生命周期、国产合规）× 4 方案（标准 Kerberos / 国密 Kerberos / OAuth 2.1 直连 / 本文）；定性+半定量（能实测填实测值，其余填依据）。
- **对比向**：方案引文（Kerberos 标准）+ 数值自实现（能实测的维度）。
- **论文位置**：5.5 + 第6章安全属性矩阵。

### 方向三文献（2026-08-19 核对更新）

- **MCP 无认证一手数据（叙事起点最佳引证）**：Zhou H, et al. A First Measurement Study on Authentication Security in Real-World Remote MCP Servers. arXiv:2605.22333, 2026——**7,973 台远程 MCP 服务器中 40.55% 无认证**、29% 静态 token/API key、30.45% OAuth；且发现未认证服务器真实泄露 CRM 敏感数据。
- **MCPS（共识佐证）**：Sharif R. MCPS: Cryptographic Security Layer for the Model Context Protocol. IETF draft-sharif-mcps-secure-mcp-00, 2026-03-14——Agent Passport + 逐消息签名 + tool 定义签名 + 重放保护，与本文方案高度同构（佐证方向正确）。
- **SMCP（共识佐证）**：Hou X, Wang S, et al. SMCP: Secure Model Context Protocol. arXiv:2602.01129, 2026——协议级统一身份+双向认证+安全上下文传播（同阶段密码学化共识）。
- OAuth 2.1 规范（RFC 9700 及 2025-03 远程化强制）；其余见八方向综述/文献库。

---

## 6. 对比试验全景（11 组 + 对比参数总表）

**对比分类三原则**（写作取数时逐组核对）：①数值自实现=同环境同负载（防"苹果比橘子"）；②方案引文=文献只定性不拿数；③内部参数=自对比支撑机制。**论文引用数值必须能在 §6 与《代码汇总版》§6 CSV 索引对应到源文件。**

### 方向一（4 组，对应实验 A1-A5）

| 实验 | 对比对象 | 对比参数 | 对比类型 | 支撑数据 |
|---|---|---|---|---|
| A1 密钥可恢复 | 自有数据自比（单图 vs 5图投票） | KRR/FAR_key/FRR_key、BER、θ 扫描 0→40、512 vs 128 维 | 内部参数 | expA1_summary / vote_gain / threshold_scan / cross_condition / impostor |
| A2 噪声鲁棒 | 自比（扰动前后） | 5 类扰动 × 强度梯度 KRR | 内部参数 | expA2_noise_krr.csv |
| A3 端到端 | 自比（熔断前后） | 成功率、阶段时延、熔断 L1/L2 | 内部参数 | expA3_*.csv |
| A4 认证性能 | **数学理论矩阵**：PKI-RSA-2048 / PKI-SM2 / 经典 IBS / 无配对 IBS / SM9（128bit 安全级归一） | 免证书/配对依赖/安全假设/等效模乘复杂度/尺寸/标准化；原语时延实测仅参考（不跨实现对比） | 理论推导 + 工程参考 | expA4_perf.csv |
| A5 攻击 | 本文 vs 口令版（同用例集） | 拦截率、熔断触发率 | **数值自实现** | expA5_*.csv |
| C5 隐私 | 传统模板存储（文献实证） | 存储内容/逆向能力/可撤销性 | 方案引文 | expA5_privacy.csv + TIFS2021/TPAMI2019 实证 |

### 方向二（4 组，对应实验 B0-B4）

| 实验 | 对比对象 | 对比参数 | 对比类型 | 支撑数据 |
|---|---|---|---|---|
| B0 组网/NAT | 自比（可行性）+ 文献[17][22] | 发现时延/拓扑/连通率；NAT 类型对直连率+兜底率 | 内部参数 + 方案引文 | expB1_discovery / nat |
| B1 准入 | **无票据 P2PVPN（PSK 基线）** | 7 类用例拦截率；三段时延；增量 | **数值自实现** | expB1_access / baseline |
| B2 扩展 | **OpenVPN（同环境）** | N=10/50/100/200：准入时延/吞吐/e2e/丢包；故障注入 | **数值自实现** | expB2_*.csv |
| B3 隧道整形 | **TLS 隧道（python ssl 自实现）**；整形前后自比 | 熵/KL/冗余率(≤15%)/时延增量/握手轮次 | 数值自实现 + 内部参数 | expB3_tunnel_shaping.csv |
| B4 攻击 | 自比（代理伪造边界） | 8 类攻击拦截率；warrant 越界 forge_ok=0 | 内部参数 | expB4_*.csv |

### 方向三（3 组，对应实验 C0-C4）

| 实验 | 对比对象 | 对比参数 | 对比类型 | 支撑数据 |
|---|---|---|---|---|
| C0 身份锚点 | 自比（4 类标识唯一性） | 唯一性/克隆去重/重注册 | 内部参数 | expC0_agent_id.csv |
| C1 分级授权 | 自比（三场景） | A/B/C 通过率；审计贯通率 | 内部参数 | expC1_*.csv |
| C2 签名链性能 | **OAuth 2.1（同环境）** | 并发 100/500/1000：p50/p90/p99/QPS/消息尺寸 | **数值自实现** | expC2_performance.csv |
| C3 攻击 | **无管控 / OAuth / 本文三方案** | ≥500 指令 × 4 类拦截率；6 类攻击矩阵；三档消融 | **数值自实现** | expC3_*.csv |
| C4 矩阵 | 标准 Kerberos / 国密 Kerberos / OAuth 2.1 / 本文 | 7 安全维度定性+半定量 | 方案引文 | expC4_security_matrix.csv |

**叙事落点**：第 3 章数值对比只有 A4 三基线（同环境），其余内部参数 + C5 文献实证链（隐私章最强论据）；第 4 章数值对比最多（PSK/OpenVPN/TLS 三基线同环境），文献负责"组合空白"定位；第 5 章三方案同环境数值对比 + MCPS/SMCP 佐证共识。

性能面/安全面指标分层见 §4。

---

## 7. 专利主成果对照（定稿口径）

主成果：CN121567387A《基于SM9与生物特征模糊提取的Kerberos身份认证系统及认证方法》（申请号 CN202511687563.7A，2025-11-18 申请，2026-02-24 公开）。详见《专利主成果对照-八方向增删改.md》。

**四大模块**：① 生物特征密钥生成模块（采集 dlib 128 维 10 张 → Min-Max 归一化 → 众数投票+稳定性评分阈值 0.8 → bio_key + RS 辅助数据 n=255,k=191,t=32 字节）；② SM9 数字身份管理模块（didsm9 DID → KGC-TEE 派生 SM9 私钥 → 凭证文件存 TEE）；③ Kerberos 协议增强模块（SM9 签名替代口令哈希，保留标准票据格式兼容；时间戳 30min）；④ 安全熔断与管理模块（行为监控→连续失败 3 次熔断→删会话/票据→恢复）。

**已定稿增删改口径**：

| 项 | 定稿 |
|---|---|
| 特征表示 | 方案叙述与专利一致（dlib 128 维）；实验采用 InsightFace 512 维提升精度，注明"特征提取器可替换" |
| 稳定比特 | 对齐专利：众数投票+稳定性评分阈值 **0.8**；主档 5 张（LFW 数据限制，专利实施例 10 张在论文注明差异） |
| 模糊提取器 | 主纠错码 **RS(255,191,t=32字节)**；重复码/BCH 为对比项 |
| 安全模板 | 降级为"可选扩展增强"（专利主方案为非存储式，不存模板） |
| 生物 DID | **`didsm9:<user_id>:<SM3 哈希>`**（实验侧全链路国密 SM3；专利原文为 SHA-256+BLAKE2b，论文注明"可替换为国密 SM3"） |
| Kerberos | 强调 AS-REQ 前置 SM9 验签完全替代口令；保留标准票据格式与加密模式，完全兼容；30min 时间戳 |
| KGC-TEE | 专利既有设计；无硬件 SGX 时用独立进程模拟 TEE，论文注明 |
| 熔断 | 专利有机制无实验——论文补实验闭环（连续 3 次→L1/L2 分层恢复） |

**专利→实验常量映射**：见《代码汇总版》§2.1。

---

## 8. 票据存储与数据流转证明

- **存储**：TGT/ST 客户端缓存（ccache），KDC 无状态；撤销靠短 TTL（30min）+熔断+重签。
- **流转证明**：ticket_id 关联链——KDC 签发生成 ticket_id → 中继验证日志 → MCP 调用携带 → 审计记录（ts, ticket_id, 双DID, tool, 判定）。方案：A ticket_id 关联链（推荐）/ B 集中审计日志 / C 中继三重验证记录。

## 9. MCP 兼容（不改 MCP 协议）

| 方案 | 做法 | 兼容性 | 改动量 |
|---|---|---|---|
| A 网关层接入 | 中继做 MCP 网关，认证在网关层 | 零改动 | 中 |
| **B 头部注入（推荐）** | 请求附 ST（header `X-ST-Ticket`） | 报文不变 | 小 |
| C 服务端中间件 | 服务端前加验证中间件（验 ST+签名链） | 部署加层 | 小 |
| D OAuth 映射 | 认证映射 MCP OAuth（ST 等价 token） | 完全生态 | 大 |

推荐 **B+C**。调用链：Agent 指令（双 DID 签名）+ 双 ST（header）→ 中继验证 → MCP 中间件验 ST+签名链 → 权限匹配（claims_checker）→ 转发 → 审计（ticket_id）。

---

## 10. 方向三联动场景（第 6 章综合）

- 用户生物认证（方向一）→ 设备入网（方向二）→ Agent 访问 MCP 服务（方向三）全链路联动：ticket_id 贯穿 KDC→中继→MCP；双 ST（ST_net/ST_data）+ 双签名链；审计全链可回溯。