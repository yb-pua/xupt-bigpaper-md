# 基于SM9与生物特征的零信任P2P网络安全机制研究（初稿素材）

> **初稿素材版**（Markdown，含真实实验数据）
> 所有实验数据来自本库 `exp1/exp2/exp3_*_results/` CSV（种子 SEED=20260817），已用真实值替换占位符。
> 数学公式使用 LaTeX 语法（`$$...$$`、`$...$`），可粘贴 Word 公式编辑器。
> 数据源标注在各实验小节【数据源】行。

---

## 摘要（待定稿）

针对传统身份认证中静态密码易泄露、身份密钥难以与生物特征直接绑定，以及传统 VPN 中心化、静态访问控制等零信任场景下安全能力不足的问题，本文研究并实现了基于 SM9 标识密码与生物特征模糊提取的零信任 P2P 网络安全机制。围绕"可信身份—可信授权—可信网络访问—可信 Human-Agent 执行—可信审计"的可信安全链，本文完成三方面工作：

（1）**核心创新——基于 SM9 与生物特征模糊提取的 Kerberos 身份认证机制**。将 128 维生物特征通过稳定 bit 选择、多数投票与 Fuzzy Extractor 构造稳定密钥 `bio_key`，进一步绑定生成 Bio-DID 与 SM9 身份密钥，实现对 Kerberos 认证的增强。实验表明，五图多数投票下生物密钥恢复率（KRR）达 0.986，异人配对误接受率（FAR）为 0，EER 0.0265、AUC 0.989；端到端认证通过率 0.967，九类攻击全部被拦截。

（2）**机制扩展——基于 ST 票据的零信任 P2P 通信机制**。以 Device-DID 与 KDC 为核心，通过 ST 票据与 Policy/Warrant 授权模型实现设备级 P2P 准入与动态访问控制。实验表明，合法节点零误拦，六类攻击 100% 拦截，准入时延 p50≈680ms 且随规模基本平稳；八类共 601 次攻击全部拦截。

（3）**应用验证——面向 MCP 的 Human-Agent 协同安全机制**。通过 Human DID、Agent DID、User ST、Agent ST 与双签名机制，建立 Agent 环境下明确的身份关系与可控授权链。实验表明，三场景审计链贯通率 1.0，六类攻击本文方案 100% 拦截而 OAuth/无管控为 0%；消融证明用户签名层与 ST 层均为必要层。

**关键词**：零信任；SM9；生物特征；模糊提取；Kerberos；P2P；MCP；去中心化身份

---

## 第一章 绪论

### 1.1 研究背景与意义

#### 1.1.1 网络安全形势与零信任安全需求

（写作要点：简述边界防御失效、内网横向渗透、VPN 集中爆破等现实背景，引出零信任"永不信任、持续验证"理念。建议引用 NIST SP 800-207 等权威文献。约 1 页。）

#### 1.1.2 传统身份认证与网络访问机制的局限

本节从三个层面总结现有机制的问题：

1. **身份认证层**：静态口令易受暴力破解、撞库与钓鱼攻击；Kerberos 等协议依赖长期密钥，一旦身份密钥泄露即失去安全性；生物特征具有天然"不可复制性"与"随身性"，但噪声敏感、无法直接稳定地作为密钥使用。
2. **网络访问层**：传统 VPN 采用中心化网关与静态访问控制策略，存在单点故障、带宽瓶颈与策略僵化问题。
3. **应用执行层**：在 MCP（Model Context Protocol）等 Human-Agent 协同场景中，Agent 身份不明确、用户授权关系弱、Tool 调用不可控，缺乏端到端的可信执行与审计链。

#### 1.1.3 本文研究意义

（写作要点：说明本文构建的"可信安全链"如何系统性地把上述三个层面的问题串起来，强调"用已有技术组合解决真实场景问题"的工程价值，不夸大理论创新。约半页。）

### 1.2 国内外研究现状

#### 1.2.1 零信任安全架构研究现状

综述 NIST SP 800-207、Google BeyondCorp、SDP 等，指出零信任的核心原则与落地难点（身份、设备、网络、应用的持续验证）。【需补齐具体文献 5~8 篇】

#### 1.2.2 SM9 标识密码及应用研究现状

综述 SM9（GB/T 38635）基于身份的加密/签名机制、国内落地案例，以及与 Kerberos/物联网认证结合的现有工作。【需补齐具体文献 5~8 篇】

#### 1.2.3 生物特征识别与模糊提取研究现状

综述生物特征模板保护（Dodis 等 Fuzzy Extractor 提出者）、可撤销生物特征、生物密钥生成（BioKey）等工作。【需补齐具体文献 5~8 篇】

#### 1.2.4 Kerberos 协议安全增强研究现状

综述 Kerberos 已知攻击（如黄金票据、凭据重放）及各类增强方案。【需补齐具体文献 3~5 篇】

#### 1.2.5 P2P 网络访问控制研究现状

综述 P2P 网络中的信任与访问控制、去中心化身份（DID/W3C DID）在 P2P 场景的应用。【需补齐具体文献 3~5 篇】

#### 1.2.6 MCP 与 Human-Agent 协同安全研究现状

综述 Agent 安全、MCP 协议安全边界、AI Agent 授权与审计相关最新工作。【需补齐具体文献 3~5 篇】

#### 1.2.7 研究现状小结与本文切入点

总结：现有研究在**单点**上各有进展，但缺乏"身份—授权—访问—执行—审计"一体化的零信任机制设计；且生物特征直接参与密码学密钥构造的研究仍较少。本文以此为切入点。

### 1.3 本文主要工作

（与摘要工作 1/2/3 对应，分三点阐述，各配一小段。约 1 页。）

### 1.4 论文组织结构

按目录叙述各章内容与逻辑关系。重点说明：第三章为核心创新，第四章为其在网络访问层的扩展，第五章为其在应用层的验证，第六章统一分析。

---

## 第二章 相关技术基础

### 2.1 零信任安全架构

简述零信任三要素（身份、设备、网络）、NIST SP 800-207 核心组件、SDP 模型。【需作图：图2.1 零信任架构示意图】

### 2.2 SM9 标识密码算法

SM9 基于双线性对，主要参数：

- $q$：大素数；
- $G_1, G_2, G_T$：三个 $q$ 阶循环群；
- 双线性对 $e: G_1 \times G_2 \to G_T$，满足双线性性、非退化性与可计算性；
- 系统主密钥 $k_{KGC} \in \mathbb{Z}_q^*$，主公钥 $P_{pub} = k_{KGC} P_1$，其中 $P_1$ 为 $G_1$ 生成元；
- 标识到整数的哈希 $H_1: \{0,1\}^* \to \mathbb{Z}_q^*$，映射 $H_2: \{0,1\}^* \to \mathbb{Z}_q^*$ 等（按 GB/T 38635 定义）。

用户标识为 $ID_A$ 时，KGC 派生私钥 $d_A$ 的过程为：

$$
t_1 = \left(H_1(ID_A \| hid, N) + k_{KGC}\right) \bmod N,
\quad
t_2 = k_{KGC} \cdot t_1^{-1} \bmod N,
\quad
d_A = t_2 \cdot P_1
$$

其中 $hid$ 为算法标识符字节，$N$ 为 $q$ 的比特表示模数，$t_1^{-1}$ 为 $t_1$ 在 $\mathbb{Z}_N^*$ 上的乘法逆元。

SM9 特点：无需证书、公钥即标识、密钥由 KGC 集中派生（本文第五章将结合 Bio-DID 讨论密钥托管问题）。

【需作图：图2.2 SM9 私钥派生与加解密/签名流程】

### 2.3 生物特征识别与模糊提取

#### 2.3.1 生物特征表示

设生物特征样本经预处理后映射为 128 维实值向量：

$$
\mathbf{x} = (x_1, x_2, \dots, x_{128}) \in \mathbb{R}^{128}
$$

同一主体的多次采样 $\mathbf{x}^{(1)}, \mathbf{x}^{(2)}, \dots$ 存在类内噪声，即对同一主体：

$$
\|\mathbf{x}^{(i)} - \mathbf{x}^{(j)}\| < \tau \quad (i \neq j)
$$

而不同主体间距离显著更大。

#### 2.3.2 Fuzzy Extractor 定义

设 $\mathcal{M}$ 为特征空间，$d: \mathcal{M} \times \mathcal{M} \to \mathbb{R}_{\ge 0}$ 为距离度量（本文采用汉明距离 $d_H$）。Fuzzy Extractor 由两个算法构成：

$$
\mathrm{Gen}: \mathcal{M} \to \{0,1\}^l \times \{0,1\}^*, \qquad
\mathrm{Rep}: \mathcal{M} \times \{0,1\}^* \to \{0,1\}^l
$$

满足：

1. **正确性**：若 $d(\omega, \omega') \le t$，则 $\mathrm{Rep}(\omega', P) = K$，其中 $(K, P) = \mathrm{Gen}(\omega)$，$P$ 为公开辅助串；
2. **安全性**：对任意可计算敌手，在不知道 $\omega'$ 的情况下，$K$ 与均匀随机 $l$ 比特串不可区分。

本文采用"纠错码 + 哈希"构造：将稳定的比特串 $\omega$ 用编码 $C$ 编码后叠加随机掩码得辅助串 $P$；重构时对 $\omega'$ 解码纠错后经哈希得到 $K$。

【需作图：图2.3 模糊提取器 Gen/Rep 原理图】

### 2.4 Kerberos 认证协议

简述 Kerberos 五步流程：用户→AS 请求 TGT→AS 返回 TGT+会话密钥→用户→TGS 请求 ST→TGS 返回 ST→用户→服务端验证。给出关键字段与时序。

**典型流程**（时序概述，完整时序表见第三章表 3-1、3-2）：

| 步骤 | 发送方 | 接收方 | 消息 | 内容 |
|---|---|---|---|---|
| 1 | Client | AS | AS_REQ | `ID_C, ID_TGS, nonce_1, Times` |
| 2 | AS | Client | AS_REP | `{K_C,TGS}_K_C, TGT={ID_C,ID_TGS,...,K_C,TGS}_K_TGS` |
| 3 | Client | TGS | TGS_REQ | `TGT, Authenticator_{K_C,TGS}, ID_S, nonce_2` |
| 4 | TGS | Client | TGS_REP | `{K_C,S}_K_C,TGS, ST={ID_C,ID_S,...,K_C,S}_K_S` |
| 5 | Client | Server | AP_REQ | `ST, Authenticator_{K_C,S}` |

【需作图：图2.4 Kerberos 基本认证流程图】

### 2.5 P2P 网络与访问控制

简述 P2P 组网方式、NAT 穿透、以及 P2P 中常见的去中心化身份与访问控制模型。引出 DID（W3C 去中心化身份标准）与本文 Device-DID 概念的联系与区别。

### 2.6 MCP 协议与 Agent 执行框架

简述 MCP（Model Context Protocol）Client/Server/Tool 模型、Human-in-the-loop、以及 Agent 执行中的身份与授权缺口。

### 2.7 本章小结

---

## 第三章 基于SM9与生物特征模糊提取的Kerberos身份认证机制（核心创新）

### 3.1 问题分析与设计目标

#### 3.1.1 传统 Kerberos 的局限

1. **静态密码风险**：用户主密钥 $K_C$ 由密码派生，密码可被爆破、撞库、钓鱼。
2. **身份密钥泄露**：长期密钥一旦泄露，攻击者可伪造票据（类比黄金票据攻击）。
3. **生物特征无法直接作为密钥**：生物特征噪声敏感，直接作为密码学密钥会产生比特不一致。

#### 3.1.2 设计目标

- G1：用生物特征稳定派生密钥，替代/弱化静态密码；
- G2：生物密钥与 SM9 身份密钥绑定，形成可验证的 Bio-DID；
- G3：不改变 Kerberos 协议主体结构，做到向后兼容；
- G4：密钥与生物模板不可被反向还原（模板保护）。

#### 3.1.3 威胁模型

敌手能力假设：敌手可窃听信道、可窃取服务器长期密钥、可尝试伪造票据；敌手不可同时获取用户生物样本与 KDC 主密钥。（逐条说明安全目标与对应机制。）

### 3.2 系统总体设计

#### 3.2.1 认证体系架构

角色：用户（User）、生物特征采集模块、Fuzzy Extractor 模块、KDC（含 AS/TGS）、服务端、KGC（SM9 密钥生成中心）。

总体架构分层：生物特征层 → 密钥层 → 身份层 → 认证协议层。

【需作图：图3.1 系统总体架构图（分层）】

#### 3.2.2 角色与组件定义

（表格列出组件、职责、部署位置。）

#### 3.2.3 身份层信任链模型

身份层信任链为：

$$
\text{生物特征} \xrightarrow{\text{稳定化}} bio\_key \xrightarrow{\text{绑定}} Bio\text{-}DID \xrightarrow{\text{派生}} \text{SM9身份密钥} \xrightarrow{\text{增强}} \text{Kerberos认证}
$$

【需作图：图3.2 身份层信任链示意图】

### 3.3 生物特征模糊提取方案设计

#### 3.3.1 128 维生物特征提取

采集样本经归一化后得到 128 维向量：

$$
\mathbf{x} = (x_1, x_2, \dots, x_{128})
$$

对训练集计算每个维度 $i$ 的类内均值 $\mu_i$ 与方差 $\sigma_i^2$。

#### 3.3.2 稳定 bit 选择

对第 $i$ 维，定义其稳定性指标：

$$
\rho_i = 1 - \frac{\sigma_i^2}{\max_{j} \sigma_j^2} \in [0, 1]
$$

选择 $\rho_i$ 最大的 $m$ 维作为稳定维度集合 $\mathcal{S}$（$m \le 128$）。对每个稳定维度做二值化：

$$
b_i = \begin{cases} 1, & x_i \ge \mu_i \\ 0, & x_i < \mu_i \end{cases} \quad (i \in \mathcal{S})
$$

得到初始稳定比特串 $\mathbf{b}^{(0)} = (b_i)_{i \in \mathcal{S}}$。

#### 3.3.3 多数投票纠错

对同一主体采集 $K$ 次样本 $\{\mathbf{x}^{(k)}\}_{k=1}^{K}$，逐维统计：

$$
\widehat{b}_i = \arg\max_{v \in \{0,1\}} \; \#\{k : b_i^{(k)} = v\}, \qquad \text{ties}\Rightarrow b_i^{(0)}
$$

其中 $b_i^{(k)}$ 为第 $k$ 次采样的第 $i$ 稳定维比特。$\widehat{\mathbf{b}} = (\widehat{b}_i)$ 作为多数投票后的稳定比特串，长度为 $m$。

（可选：对投票一致率低于阈值 $\theta$ 的维度剔除，进一步降噪。）

#### 3.3.4 Fuzzy Extractor 构造

采用纠错码 $C \subseteq \{0,1\}^m$（如 BCH 码，可纠 $t$ 位错），构造：

- **Gen**：随机取 $\mathbf{c} \leftarrow C$，令辅助串 $P = \mathbf{c} \oplus \widehat{\mathbf{b}}$，密钥 $K = \mathrm{H}(\widehat{\mathbf{b}})$，其中 $\mathrm{H}: \{0,1\}^m \to \{0,1\}^l$ 为密码学哈希；
- **Rep**：对输入的 $\widehat{\mathbf{b}}'$，计算 $\mathbf{c}' = \mathrm{Decode}(P \oplus \widehat{\mathbf{b}}')$；若解码成功（$\mathbf{c}' \in C$）且 $d_H(\widehat{\mathbf{b}}', \widehat{\mathbf{b}}) \le t$，则输出 $K' = \mathrm{H}(\widehat{\mathbf{b}}')$ 与 $K$ 一致。

正确性：当 $d_H(\widehat{\mathbf{b}}', \widehat{\mathbf{b}}) \le t$ 时，$P \oplus \widehat{\mathbf{b}}' = \mathbf{c} \oplus (\widehat{\mathbf{b}} \oplus \widehat{\mathbf{b}}')$，其中 $\widehat{\mathbf{b}} \oplus \widehat{\mathbf{b}}'$ 为 ≤ $t$ 位的噪声，可由 $C$ 的纠错能力修正，得到 $\mathbf{c}' = \mathbf{c}$。

#### 3.3.5 bio_key 生成

最终生物密钥：

$$
bio\_key = K = \mathrm{H}(\widehat{\mathbf{b}}) \in \{0,1\}^l
$$

$l$ 取 256 位。辅助串 $P$ 可公开存储，不泄露 $\widehat{\mathbf{b}}$（当 $C$ 具有足够信息论安全余量时）。

【需作图：图3.3 生物特征模糊提取流程（稳定bit→投票→FE→bio_key）】

【需制表：表3-1 模糊提取参数配置（m、K、编码、t、l 等）】

### 3.4 Bio-DID 与 SM9 身份密钥绑定

#### 3.4.1 Bio-DID 设计

定义基于生物密钥的 DID：

$$
Bio\text{-}DID = \text{did:bio:} \operatorname{base58}\bigl(\mathrm{H}(bio\_key \| \text{rand})\bigr)
$$

其中 $\text{rand}$ 为注册时随机盐，用于防止同名样本碰撞与离线枚举。Bio-DID 语义上代表"持有该生物密钥的实体"。

#### 3.4.2 SM9 身份密钥派生与注册

用户向 KGC 注册，以 $ID_{Bio} = Bio\text{-}DID$ 作为 SM9 标识，KGC 派生私钥：

$$
d_{Bio} = \frac{k_{KGC}}{H_1(ID_{Bio} \| hid, N) + k_{KGC}} \cdot P_1
$$

即沿用 2.2 节公式，令 $ID_A = ID_{Bio}$。注册流程（时序表）：

**表 3-2 SM9 身份密钥注册时序**

| 步骤 | 发送方 | 接收方 | 消息/操作 | 关键内容 |
|---|---|---|---|---|
| 1 | User | KGC | 注册请求 | `ID_{Bio}`（Bio-DID） |
| 2 | KGC | KGC | 验证 | 校验用户持有 bio_key 的证明（可选一次挑战响应） |
| 3 | KGC | KGC | 派生 | 按 2.2 节计算 $d_{Bio}$ |
| 4 | KGC | User | 返回 | 安全通道下发 $d_{Bio}$ |
| 5 | User | 本地 | 存储 | $d_{Bio}$ 与 bio_key 绑定存储（加密） |

#### 3.4.3 密钥生命周期管理

说明：bio_key 随生物样本更新（重注册）、吊销、设备绑定等管理策略。强调生物模板与生物密钥的**不可逆**分离（模板保护）。

### 3.5 增强 Kerberos 认证流程设计

#### 3.5.1 用户注册流程

用户在 AS 注册，绑定 Bio-DID 与 bio_key 派生能力（注册时序见 3.4.2）。

#### 3.5.2 身份认证与 TGT/ST 获取流程

以 $bio\_key$ 作为用户主密钥来源，替代静态密码派生 $K_C$。增强后的流程：

**表 3-3 增强 Kerberos 认证时序（TGT 获取）**

| 步骤 | 发送方 | 接收方 | 消息/操作 | 关键内容 |
|---|---|---|---|---|
| 1 | User | 本地 | 生物采集+FE | 生成 $bio\_key$（每次认证现场重算） |
| 2 | User | AS | AS_REQ | `ID_{Bio}, ID_{TGS}, nonce_1, Times`，并用 $K_C = \mathrm{H}(bio\_key)$ 派生会话请求 |
| 3 | AS | User | AS_REP | 用 $K_C$ 加密返回 `TGT, K_{C,TGS}` |
| 4 | User | 本地 | 解密 | 用现场 $bio\_key$ 派生 $K_C$ 解密 |
| 5 | User | TGS | TGS_REQ | `TGT, Authenticator_{K_{C,TGS}}, ID_S, nonce_2` |
| 6 | TGS | User | TGS_REP | 返回 `ST, K_{C,S}` |
| 7 | User | Server | AP_REQ | `ST, Authenticator_{K_{C,S}}` |

要点：$K_C = \mathrm{H}(bio\_key)$ 不再存储在磁盘；每次认证现场由生物特征重构，体现"生物特征作为密钥"。

#### 3.5.3 会话密钥建立流程

沿用 Kerberos 会话密钥协商机制（$K_{C,S}$），本文不再改动，仅在入口处替换密钥来源。【需作图：图3.4 增强 Kerberos 认证全流程时序图】

### 3.6 安全性分析

#### 3.6.1 抗静态密码攻击分析

原系统攻击面：密码字典空间 $|\mathcal{P}|$ 有限，可离线爆破。本文密钥空间由 $bio\_key$ 决定，其熵来源为特征稳定比特与哈希输出的混合：

$$
H_\infty(bio\_key) \approx \min_{i \in \mathcal{S}} H_\infty(b_i) \cdot |\mathcal{S}| \quad \text{（受维度相关性制约）}
$$

离线爆破需同时通过 FE 正确性与哈希校验，攻击代价显著高于字典爆破。**注：本文不宣称生物特征熵等同于密码学熵，仅量化其对抗暴力枚举的提升。实验测得 W 熵 74.4 bit，即暴力破解步数上界 $\log_2$ 值约为 74.4（数据源 expA5_privacy.csv）。**

#### 3.6.2 抗身份密钥泄露分析

若 $d_{Bio}$ 泄露，敌手可获得一次有效 SM9 私钥；但 bio_key 仍为独立随机源，可重新注册新 Bio-DID 使旧密钥失效（密钥隔离与可撤销性）。实验测得重登记后辅助数据差异率 86.3%，即新旧身份不可链接（数据源 expA5_privacy.csv）。

#### 3.6.3 抗生物特征模板攻击分析

辅助串 $P = \mathbf{c} \oplus \widehat{\mathbf{b}}$。若 $C$ 的最小距离为 $d_{\min}$，敌手从 $P$ 恢复 $\widehat{\mathbf{b}}$ 的等价问题为在有噪掩码下解码，信息论余量：

$$
R = m - \log_2 |C|
$$

当 $R$ 足够大时 $P$ 不泄露足够模板信息。实验测得辅助数据每字节熵 7.08 bit（总 1804 bit），接近每字节 8 bit 的均匀上限，说明 $P$ 几乎不含模板结构信息（数据源 expA5_privacy.csv）。

#### 3.6.4 与现有方案安全属性对比

**表 3-4 身份认证方案安全属性对比**

| 安全属性 | 传统密码+Kerberos | 生物特征直接模板匹配 | 本文方案 |
|---|---|---|---|
| 抗离线爆破 | 弱 | 中（模板保护缺失则弱） | 中高（W 熵 74.4 bit） |
| 密钥泄露可恢复 | 弱（密码难改） | — | 强（可重注册，差异率 86.3%） |
| 模板保护 | — | 弱 | 强（辅助串熵 7.08 B/字节） |
| 与 Kerberos 兼容 | 强 | 弱 | 强 |
| 生物特征隐私 | — | 弱 | 中高 |

### 3.7 实验与验证

#### 3.7.1 实验环境与数据集

- 硬件：host-cpu（【需按实际机器补全型号】）
- 数据集：生物特征数据集，队列人数 417，跨条件 1254 人次（数据源 expA1_summary.csv）
- 实现：gmalg-1.1.2（SM9 纯 Python 实现）、cryptography 库
- 种子：SEED=20260817（全实验固定，可复现）
- 基线：传统密码 Kerberos、直接模板匹配方案

#### 3.7.2 特征稳定性实验（生物密钥可恢复性）

【数据源 expA1_*.csv】

| 指标 | 单图 | 五图投票 |
|---|---|---|
| 密钥恢复率 KRR | 0.287 | **0.986** |
| BER 均值（字节） | 29.4 | 15.3 |
| BER p95（字节） | 32.0 | 23.0 |
| 异人配对 FAR | 0.000 | 0.000 |
| EER / AUC | — | 0.0265 / 0.989 |

结论：多数投票显著提升 KRR（0.287→0.986），异人配对零误接受。

**噪声鲁棒性（100 人缩减版）**

【数据源 expA2_*.csv】

| 扰动类型 | KRR | 平均相似度 | 最差档 KRR |
|---|---|---|---|
| brightness 亮度 | 0.990 | 0.993 | 0.990 |
| rotation 旋转 | 0.988 | 0.990 | 0.980 |
| blur 模糊 | 0.838 | 0.881 | 0.450 |
| gaussian 高斯噪声 | 0.645 | 0.943 | 0.000 |
| occlusion 遮挡 | 0.167 | 0.558 | 0.000 |

结论：亮度/旋转鲁棒，模糊/高斯/遮挡在强扰动档位明显退化——**需在讨论中如实说明局限**。

【需作图：图3.5 不同扰动下 KRR 曲线（用 expA2 数据）】

#### 3.7.3 认证性能实验

【数据源 expA4_*.csv，n=200，纯 Python gmalg】

| 原语 | 操作 | 中位耗时 (ms) | p95 (ms) |
|---|---|---|---|
| SM9 | sign / verify | 193.5 / 334.0 | 277.3 / 686.3 |
| SM4-CBC | encrypt / decrypt | 0.69 / 0.50 | 0.74 / 0.53 |
| SM3 | hash | 0.65 | 0.88 |
| RSA-2048（对照） | sign / verify | 0.61 / 0.028 | 0.99 / 0.047 |
| ECDSA-P256（对照） | sign / verify | 0.027 / 0.075 | 0.042 / 0.097 |
| SM2（对照） | sign / verify | 19.1 / 34.5 | 201.4 / 371.5 |

说明：SM9 为纯 Python 实现，绝对值偏高；与同为国密曲线的 SM2（19.1/34.5ms）相比处于同一数量级但有差距，需在讨论中说明"实现方式影响"，避免结论过度。

【需作图：图3.6 签名/验签时延对比（SM9 vs SM2 vs ECDSA vs RSA）】

#### 3.7.4 端到端认证与攻击验证

**端到端流程**（30 人）【数据源 expA3_summary.csv】：

| 指标 | 值 |
|---|---|
| 端到端流程通过率 | 0.967 |
| 熔断 L1（票据清理+Rep 重认证） | 通过 |
| 熔断 L2（重新 Gen+更新登记） | 通过 |
| L2 后重新认证 | 通过 |

**攻击验证**（9 类）【数据源 expA5_attacks.csv】：

| 攻击 | 拦截机制 |
|---|---|
| a01 重放 AS_REQ | nonce 注册表 |
| a02/a03 篡改 ST/TGT | SM4-CBC 完整性 |
| a04 伪造注册签名 | SM9 验签 |
| a05 未知 DID | 注册表 |
| a06 伪造 TGS 认证器 | 会话密钥解密 |
| a07 冒用生物 Rep | 密钥哈希 |
| a08 过期票据 | 票据有效期 |
| a09 时钟偏移 | 时间戳窗口 |

九类攻击全部拦截，成功数 0。

### 3.8 本章小结

---

## 第四章 基于ST票据的零信任P2P通信机制（机制扩展）

### 4.1 问题分析与设计目标

#### 4.1.1 传统 VPN 的局限

中心化网关单点故障、静态访问控制策略无法适应动态环境、网关成为攻击目标。

#### 4.1.2 设计目标与威胁模型

- G1：设备身份化（Device-DID）；
- G2：授权动态化（Policy/Warrant + 票据）；
- G3：通信去中心化（P2P，不依赖中心网关转发数据面）；
- 威胁模型：内网敌手、伪造设备、票据伪造/重放等。

### 4.2 系统总体设计

#### 4.2.1 Device-DID 与设备身份模型

每台设备注册 Device-DID，与设备证书/指纹绑定。【需作图：图4.1 P2P 通信系统总体架构】

#### 4.2.2 KDC 与 ST 票据机制

复用第三章 KDC，为设备签发服务票据 ST，作为设备间通信的准入凭证。

#### 4.2.3 Policy/Warrant 授权模型

定义授权凭据（Warrant）：

$$
Warrant = (Subject_{DID}, Object_{DID}, Action, Resource, ValidFrom, ValidTo, \sigma_{KDC})
$$

其中 $\sigma_{KDC}$ 为 KDC 签名。

#### 4.2.4 P2P 准入与安全通信总体流程

设备 A 与设备 B 通过 ST 与 Warrant 完成双向准入，再建立端到端加密通道。

### 4.3 关键机制设计

#### 4.3.1 设备身份注册与授权策略绑定

**表 4-1 设备注册与策略绑定时序**

| 步骤 | 发送方 | 接收方 | 消息/操作 | 关键内容 |
|---|---|---|---|---|
| 1 | Device A | KDC | 注册 | 设备指纹+公钥 → Device-DID |
| 2 | KDC | Device A | 返回 | 设备凭证、DID 文档 |
| 3 | Admin | KDC | 策略下发 | 为 Device A 生成 Policy |
| 4 | KDC | 本地 | 存储 | Policy ↔ Device-DID 绑定 |

#### 4.3.2 ST 票据签发与验证流程

**表 4-2 ST 票据签发时序**

| 步骤 | 发送方 | 接收方 | 消息/操作 | 关键内容 |
|---|---|---|---|---|
| 1 | Device A | TGS | TGS_REQ | `Device-DID_A, ID_S(=Device-DID_B), nonce` |
| 2 | TGS | 本地 | 策略检查 | 校验 Policy/Warrant 是否满足访问 B |
| 3 | TGS | Device A | TGS_REP | `ST = {A,B,K_{AB},有效期}_K_B, K_{A,B}` |
| 4 | Device A | Device B | AP_REQ | `ST, Authenticator` |
| 5 | Device B | 本地 | 验证 | 验 ST 签名、有效期、Authenticator |

#### 4.3.3 P2P 对等安全通道建立

在 ST 验证通过后，双方以 $K_{AB}$ 建立端到端加密（如 DTLS/AEAD），并协商会话密钥，数据面不经过中心网关。

#### 4.3.4 动态访问控制与票据撤销

策略变化时 KDC 吊销或刷新 Warrant/ST；设备侧缓存票据需设置短时效。会话级撤销可结合审计端联动（见第五章）。

### 4.4 安全性分析

- 防票据伪造：ST 由 KDC 签名，敌手无法在无 KDC 私钥时伪造；
- 防重放：Authenticator 含时间戳与随机数，短时有效期；
- 去中心化收益：数据面不依赖网关，单点故障与网关爆破风险下降；
- 局限：控制面仍依赖 KDC（**本文不宣称完全去中心化**，KDC 为信任锚点）。

### 4.5 实验与验证

#### 4.5.1 发现服务与 NAT 打洞

【数据源 expB1_discovery.csv / expB1_nat.csv】四类 NAT（full_cone / restricted / port_restricted / symmetric）两两组合打洞判定；中继兜底比例 0.375，直连理论比例 0.625。

#### 4.5.2 组网准入

【数据源 expB1_*.csv】7 类用例：合法节点不误拦（block_rate=0），expired / replay / forged_auth / tampered_st / did_spoofing / unbound 六类攻击 100% 拦截。准入时延 p50=680ms（三段分解：auth 160 / st 161 / challenge 160 ms）。

#### 4.5.3 扩展性与 OpenVPN 基线

【数据源 expB2_*.csv】

| 规模 N | 准入 p50 (ms) | 吞吐 (Mbps) | 丢包 |
|---|---|---|---|
| 10 / 50 / 100 / 200 | ≈712（平稳） | 0.7 → 5.7 | 0 |

- 准入时延随规模平稳（≈712ms），吞吐随节点增多近似线性增长。
- OpenVPN 控制面握手基线 p50=110.1ms（**数据面为 simulated，需在论文中标注**）。

【需作图：图4.2 准入时延与吞吐随规模变化】

#### 4.5.4 隧道流量整形

【数据源 expB3_*.csv】

| 业务流 | 包长熵（raw → shaped） | KL 散度 | 冗余率 |
|---|---|---|---|
| period 周期流 | 0 → 3.28 | 34.99 | 91.8% |
| request 请求流 | 1.0 → 3.56 | 33.75 | 183.9% |
| burst 突发流 | 0 → 0（1448B 无填充空间） | — | 0% |

说明：周期/请求流填充后熵显著提升，突发流因最大包长无填充空间，流量整形无效——**需如实说明该项局限**。

#### 4.5.5 攻击面验证

【数据源 expB4_*.csv】八类攻击共 601 次，全部 100% 拦截；代理伪造 5 项用例全部符合预期。

【需制表：表4-3 八类攻击拦截明细】

### 4.6 本章小结

---

## 第五章 面向MCP的Human-Agent协同安全机制（应用验证）

### 5.1 问题分析与设计目标

#### 5.1.1 Agent 环境安全问题

1. **身份不明确**：Agent 没有独立的可验证身份；
2. **用户授权关系弱**：用户与 Agent 的授权绑定不清晰；
3. **Tool 调用不可控**：Agent 调用外部工具缺乏细粒度授权与审计。

#### 5.1.2 设计目标与威胁模型

- G1：Agent 身份化（Agent DID + Human DID 绑定）；
- G2：授权链可验证（User ST + Agent ST）；
- G3：执行可审计（双签名 + 审计链）。

### 5.2 安全模型设计

#### 5.2.1 Human DID 与 Agent DID

用户注册 Human DID（可复用第三章 bio_key 机制），Agent 注册 Agent DID 并绑定其"所有者" Human DID。

#### 5.2.2 User ST 与 Agent ST

- **User ST**：用户授予 Agent 的票据，声明"允许该 Agent 在用户授权范围内行动"；
- **Agent ST**：Agent 每次 Tool 调用签发的票据，携带调用意图与参数摘要。

#### 5.2.3 双签名机制

每次 Tool 调用需双重签名：

$$
\sigma_{total} = \mathrm{Sign}_{SK_{Human}}(h) \,\|\, \mathrm{Sign}_{SK_{Agent}}(h),
\qquad h = \mathrm{H}(tool\_id \| args \| user\_st \| agent\_st \| ts)
$$

其中 $h$ 为调用摘要哈希。只有 Human（授权）与 Agent（执行）同时签名才视为有效调用，实现"授权与执行分离"。

### 5.3 可信 Agent 执行与审计流程

#### 5.3.1 授权链建立流程

**表 5-1 Human-Agent 授权链建立时序**

| 步骤 | 发送方 | 接收方 | 消息/操作 | 关键内容 |
|---|---|---|---|---|
| 1 | User | 系统 | 创建授权 | 生成 User ST（含范围、有效期） |
| 2 | 系统 | Agent | 下发 | Agent 绑定 User ST |
| 3 | Agent | 系统 | 注册 | Agent DID + 公钥 + 授权范围 |
| 4 | 系统 | 审计 | 记录 | 授权链事件入库 |

#### 5.3.2 Tool 调用控制流程

**表 5-2 双签名 Tool 调用时序**

| 步骤 | 发送方 | 接收方 | 消息/操作 | 关键内容 |
|---|---|---|---|---|
| 1 | Agent | 授权网关 | 调用请求 | `tool_id, args, agent_st` |
| 2 | 授权网关 | 用户端 | 授权确认 | 双签名请求 `h` |
| 3 | 用户端 | 授权网关 | 返回 | `Sign_{SK_Human}(h)` |
| 4 | Agent | 授权网关 | 提交 | `Sign_{SK_Agent}(h)` + 摘要 |
| 5 | 授权网关 | Tool Server | 执行 | 校验双签名后放行 |
| 6 | 授权网关 | 审计 | 记录 | 调用+双签名+结果入审计 |

#### 5.3.3 执行过程审计与追溯

每次调用产生审计记录 $\mathcal{R} = (h, \sigma_{total}, result\_hash, ts)$，支持事后追溯与异常检测。

### 5.4 安全性分析

- 身份明确性：Agent 不再匿名，具备 DID；
- 授权关系强度：User ST 声明授权边界，双签名保证"用户同意 + Agent 执行"；
- Tool 调用可控性：每调用一次授权校验一次，支持最小权限；
- 审计完整性：双签名与结果哈希保证记录不可抵赖。

### 5.5 实验与验证

#### 5.5.1 环境标识锚定

【数据源 expC0_agent_id.csv】4 类环境（docker/linux/windows/vmware）200 个设备 DID 零冲突；克隆同标识 → 同 DID 被识别。

#### 5.5.2 场景与审计链

【数据源 expC1_*.csv】三场景（数据查询 / 协作链 / 网关跨域）全部通过；审计链 ticket_id 贯通率 1.0。

| 场景 | 步骤 | 通过 | 时延 (ms) |
|---|---|---|---|
| A_data_query | 4 | 是 | 1283.6 |
| B_collab_chain | 8 | 是 | 1281.7 |
| C_cross_domain | 2 | 是 | 566.4 |

#### 5.5.3 性能（趋势性对比）

【数据源 expC2_performance.csv】**本文方案时延受 gmalg 纯 Python + GIL 串行限制，仅作安全性代价的趋势性对比，非绝对性能。**

| 方案 | 并发 | p50 | qps | 报文字节 |
|---|---|---|---|---|
| 本文（SM9 双 ST + 双签名链） | 100 / 500 | 89.2s / 497.4s | 0.81 / 0.76 | 2261 / 2262 B |
| OAuth 基线 | 100 / 500 | 0.0005ms / 0.0009ms | 102643 / 30093 | 115 B |

【需作图：图5.1 双签名方案 vs OAuth 时延/QPS 对比（注明趋势性）】

#### 5.5.4 攻击与消融

【数据源 expC3_*.csv】六类攻击（tampered / priv_esc / replay / forged_st / did_spoof / confusion）：本文 100% 拦截，OAuth 与无管控 0% 拦截。

| 攻击类型 | 本文 | OAuth | 无管控 |
|---|---|---|---|
| tampered / priv_esc / replay / forged_st / did_spoof / confusion | 1.00 | 0.00 | 0.00 |

消融：完整方案正常 0% 误拦、攻击 100% 拦截；去掉用户签名层或 ST_data 层后，正常调用 100% 失败（证明二者为必要层）。

#### 5.5.5 安全矩阵

【数据源 expC4_security_matrix.csv】

| 维度 | Kerberos 标准 | 国密 Kerberos | OAuth 2.1 | 本文 |
|---|---|---|---|---|
| 抗重放 | 1 | 1 | 0 | 1 |
| 抗伪造 | 1 | 1 | 1 | 1 |
| 权限粒度 | 0 | 0 | 0 | 1 |
| 调用者绑定 | 0 | 0 | 0 | 1 |
| 审计归因 | 0 | 0 | 0 | 1 |
| 吊销 | 1 | 1 | 1 | 1 |
| 国密合规 | 0 | 1 | 0 | 1 |

结论：本文方案在权限粒度、调用者绑定、审计归因三方面补齐了 Kerberos/OAuth 的缺失能力。

【需作图：图5.2 MCP Human-Agent 协同安全架构】【需作图：图5.3 双签名 Tool 调用时序图】

### 5.6 本章小结

---

## 第六章 统一分析与系统验证

### 6.1 可信安全链统一框架

将三章机制整合为统一可信链：

$$
\underbrace{可信身份}_{\text{第三章}} \to \underbrace{可信授权}_{3.4/5.2} \to \underbrace{可信网络访问}_{\text{第四章}} \to \underbrace{可信执行}_{\text{第五章}} \to \underbrace{可信审计}_{\text{第五章}}
$$

【需作图：图6.1 可信安全链统一架构总图】

### 6.2 安全机制统一对比分析

#### 6.2.1 安全目标覆盖度对比

**表 6-1 三方向机制安全目标覆盖矩阵**

| 安全目标 | 方向一 | 方向二 | 方向三 |
|---|---|---|---|
| 身份可信 | ✓（核心） | ✓（Device-DID） | ✓（Human/Agent DID） |
| 授权可信 | 部分 | ✓（Warrant） | ✓（User/Agent ST） |
| 访问可控 | — | ✓（ST 准入） | 部分 |
| 执行可控 | — | — | ✓（双签名） |
| 可审计 | — | 部分 | ✓ |
| 抗重放/伪造 | ✓（9/9 攻击拦截） | ✓（6/6 攻击拦截） | ✓（6/6 攻击拦截） |

#### 6.2.2 与现有零信任/认证方案对比

**表 6-2 本文与现有方案整体对比**

| 方案 | 身份层 | 网络层 | 应用层 | 审计 |
|---|---|---|---|---|
| 传统 VPN | 证书/口令 | 静态网关 | 无 | 弱 |
| 标准零信任（BeyondCorp 等） | 设备+用户 | 动态 | 弱 | 中 |
| 本文方案 | 生物+SM9 | ST 票据 P2P | 双签名 | 强 |

### 6.3 系统级实验验证

- 端到端流程验证：方向一 30 人端到端通过率 0.967；方向三三场景审计链贯通率 1.0。
- 安全验证：方向一 9/9、方向二 601 次/601、方向三 6/6 攻击全部拦截。
- 性能：方向一 SM9 纯 Python 下 sign/verify 193.5/334.0ms；方向二准入 p50≈680ms 且规模平稳；方向三为趋势性对比（GIL 限制）。
- 综合结论与局限见 6.4。

### 6.4 讨论

#### 6.4.1 机制局限性（必须如实表述）

1. **性能**：SM9 为纯 Python 实现（gmalg），sign/verify 中位 193.5/334.0ms，方向三并发场景受 GIL 串行限制导致高时延——不构成对 SM9 算法本身的性能结论，需以 C 实现/硬件加速后再评估；
2. **信任锚点**：KDC/KGC 仍为信任锚点，非完全去中心化；
3. **生物特征熵**：W 熵 74.4 bit 为实验测得，但生物特征熵不能等同密码学熵，且遮挡/高斯强扰动下 KRR 显著下降（0.167 / 0.645）；
4. **流量整形局限**：burst 流无填充空间，整形无效；
5. **OpenVPN 数据面为 simulated**，仅控制面握手为实测。

#### 6.4.2 适用场景与边界

适用于对身份真实性、审计可追溯性要求高的内网/协同场景；不适用于无信任锚点的完全开放网络。

### 6.5 本章小结

---

## 第七章 总结与展望

### 7.1 工作总结

（按三条研究线总结，强调"组合创新、工程落地、结论对应实验"。实验结论要点：方向一 KRR 0.986 / FAR 0 / EER 0.0265 / 9 类攻击全拦截；方向二准入 p50≈680ms / 攻击 100% 拦截 / 吞吐 0.7→5.7Mbps；方向三审计贯通率 1.0 / 攻击 100% 拦截 vs OAuth 0%。）

### 7.2 研究不足

1. 生物特征熵的理论量化与更大规模数据集验证不足（当前队列 417 人）；
2. SM9 纯 Python 实现性能不代表算法真实水平，缺 C/硬件加速基准；
3. KGC/KDC 单点信任的替代方案未深入；
4. 流量整形对突发流无效、方向三性能仅趋势性对比。

### 7.3 未来展望

- 生物密钥与硬件安全模块（TPM/SE）结合，SM9 采用硬件/库实现做真实性能基准；
- 门限 KGC 实现分布式密钥管理；
- MCP 场景扩大至多 Agent 协作与跨组织授权；
- 更大规模生物数据集与真实网络环境部署验证。

---

## 附录

### 附录 A：图表需求清单

**图（按优先级）**

| 编号 | 章节 | 内容 | 类型 | 数据源 |
|---|---|---|---|---|
| 图2.1 | 2.1 | 零信任架构示意图 | 架构图 | — |
| 图2.2 | 2.2 | SM9 私钥派生与签名流程 | 流程图 | — |
| 图2.3 | 2.3 | 模糊提取器 Gen/Rep 原理 | 原理图 | — |
| 图2.4 | 2.4 | Kerberos 基本认证流程 | 时序图 | — |
| 图3.1 | 3.2 | 认证体系总体架构（分层） | 架构图 | — |
| 图3.2 | 3.2 | 身份层信任链示意 | 链式图 | — |
| 图3.3 | 3.3 | 生物特征模糊提取流程 | 流程图 | — |
| 图3.4 | 3.5 | 增强 Kerberos 认证时序 | 时序图 | — |
| 图3.5 | 3.7.2 | 不同扰动下 KRR 曲线 | 实验图 | expA2 |
| 图3.6 | 3.7.3 | 签名/验签时延对比 | 实验图 | expA4 |
| 图4.1 | 4.2 | P2P 通信系统总体架构 | 架构图 | — |
| 图4.2 | 4.5.3 | 准入时延与吞吐随规模变化 | 实验图 | expB2 |
| 图5.1 | 5.5.3 | 双签名 vs OAuth 性能对比（趋势性） | 实验图 | expC2 |
| 图5.2 | 5.5 | MCP Human-Agent 协同安全架构 | 架构图 | — |
| 图5.3 | 5.5 | 双签名 Tool 调用时序 | 时序图 | — |
| 图6.1 | 6.1 | 可信安全链统一架构总图 | 架构图 | — |

**表（已在正文给出模板或真实数据）**

| 编号 | 章节 | 内容 |
|---|---|---|
| 表2-1 | 2.4 | Kerberos 五步流程表 |
| 表3-1 | 3.3 | 模糊提取参数配置 |
| 表3-2 | 3.4 | SM9 身份密钥注册时序 |
| 表3-3 | 3.5 | 增强 Kerberos 认证时序 |
| 表3-4 | 3.6 | 认证方案安全属性对比（含真实熵数据） |
| 表3-5 | 3.7.2 | 生物密钥可恢复性（真实数据） |
| 表3-6 | 3.7.2 | 噪声鲁棒性（真实数据） |
| 表3-7 | 3.7.3 | 密码原语性能对比（真实数据） |
| 表3-8 | 3.7.4 | 端到端流程+9类攻击（真实数据） |
| 表4-1 | 4.3 | 设备注册与策略绑定时序 |
| 表4-2 | 4.3 | ST 票据签发时序 |
| 表4-3 | 4.5.1 | NAT 打洞结果（真实数据） |
| 表4-4 | 4.5.2 | 组网准入+攻击拦截（真实数据） |
| 表4-5 | 4.5.3 | 扩展性与 OpenVPN 对比（真实数据） |
| 表4-6 | 4.5.4 | 流量整形结果（真实数据） |
| 表5-1 | 5.3 | Human-Agent 授权链时序 |
| 表5-2 | 5.3 | 双签名 Tool 调用时序 |
| 表5-3 | 5.5.2 | 三场景审计链（真实数据） |
| 表5-4 | 5.5.3 | 性能趋势性对比（真实数据） |
| 表5-5 | 5.5.4 | 攻击+消融（真实数据） |
| 表5-6 | 5.5.5 | 安全矩阵 7 维×4 方案（真实数据） |
| 表6-1 | 6.2 | 三方向安全目标覆盖矩阵 |
| 表6-2 | 6.2 | 与现有方案整体对比 |

### 附录 B：数据源索引（全部可复现，SEED=20260817）

| 结果 | 对应 CSV |
|---|---|
| A1 可恢复性 | expA1_summary / vote_gain / threshold_scan / cross_condition / impostor.csv |
| A2 噪声鲁棒 | expA2_noise_krr / expA2_summary.csv |
| A3 端到端 | expA3_flow / circuit / summary.csv |
| A4 性能 | expA4_perf / summary.csv |
| A5 攻击+隐私 | expA5_attacks / privacy / summary.csv |
| B1 准入 | expB1_access_results / baseline / discovery / nat / summary.csv |
| B2 扩展性 | expB2_scalability / failover / summary.csv |
| B3 整形 | expB3_tunnel_shaping / summary.csv |
| B4 攻击 | expB4_attack_results / proxy_forge / summary.csv |
| C0 环境锚定 | expC0_agent_id.csv |
| C1 场景审计 | expC1_scenarios / audit.csv |
| C2 性能 | expC2_performance.csv |
| C3 攻击消融 | expC3_attack_matrix / ablation / instruction_results.csv |
| C4 安全矩阵 | expC4_security_matrix.csv |

### 附录 C：待补参考文献方向

- 零信任：NIST SP 800-207；Google BeyondCorp（2014）；SDP（CSA）。
- SM9：GB/T 38635.1/.2；相关 SM9 应用论文。
- 生物特征模板保护 / 模糊提取：Dodis et al.（Eurocrypt 2004）。
- Kerberos：RFC 4120；黄金票据/白银票据攻击分析。
- P2P / DID：W3C DID Core。
- MCP / Agent 安全：Model Context Protocol 规范；AI Agent 安全与授权审计最新工作。