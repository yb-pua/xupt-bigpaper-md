# 实验结果汇总（方向一 ~ 方向三）

> 本文件汇总三个方向的全部实验结果。原始 CSV 数据见各 `*_results/` 目录，可直接用于论文取数。

---

## 方向一：基于 SM9 与生物特征模糊提取的 Kerberos 身份认证

对应专利 CN121567387A 四大模块的实验验证。

### A1 生物密钥可恢复性
| 指标 | 值 |
|---|---|
| 队列人数 / 跨条件人数 | 417 / 1254 |
| 单图 KRR | 0.287 |
| 五图投票 KRR | 0.986 |
| 单图 BER（均值 / p95） | 29.4 / 32.0 字节 |
| 五图 BER（均值 / p95） | 15.3 / 23.0 字节 |
| 异人配对 FAR | 0.000 |
| EER / AUC | 0.0265 / 0.989 |

### A2 噪声鲁棒性（100 人缩减版）
| 扰动类型 | KRR（密钥恢复率） | 平均相似度 | 最差档 KRR |
|---|---|---|---|
| brightness 亮度 | 0.990 | 0.993 | 0.990 |
| rotation 旋转 | 0.988 | 0.990 | 0.980 |
| blur 模糊 | 0.838 | 0.881 | 0.450 |
| gaussian 高斯噪声 | 0.645 | 0.943 | 0.000 |
| occlusion 遮挡 | 0.167 | 0.558 | 0.000 |

### A3 端到端认证 + 熔断
| 指标 | 值 |
|---|---|
| 端到端流程通过率 | 0.967 |
| 熔断 L1（票据清理 + Rep 重认证） | 通过 |
| 熔断 L2（重新 Gen + 更新登记） | 通过 |
| L2 后重新认证 | 通过 |

### A4 性能对比（gmalg 纯 Python）
| 原语 | 操作 | 中位耗时 |
|---|---|---|
| SM9 | sign / verify | 193.5ms / 334.0ms |
| SM4-CBC | encrypt / decrypt | 0.69ms / 0.50ms |
| SM3 | hash | 0.65ms |
| RSA-2048（对照） | sign / verify | 0.61ms / 0.028ms |
| ECDSA-P256（对照） | sign / verify | 0.027ms / 0.075ms |
| SM2（对照） | sign / verify | 19.1ms / 34.5ms |

### A5 攻击 + 隐私
| 指标 | 值 |
|---|---|
| 攻击类型 / 拦截数 | 9 类 / 9 类全部拦截 |
| 辅助数据 σ 熵 | 7.08 字节 |
| 重登记 σ 差异率 | 86.3%（不可链接） |
| W 熵 | 74.4 bit |

---

## 方向二：基于 SM9 的国密 P2P VPN

### B0 发现服务 + NAT 打洞
- 四类 NAT（full_cone / restricted / port_restricted / symmetric）两两组合打洞判定；中继兜底比例 0.375，直连理论比例 0.625。

### B1 组网准入
- 7 类用例：合法节点不误拦（block_rate=0），expired / replay / forged_auth / tampered_st / did_spoofing / unbound 六类攻击 100% 拦截。
- 准入时延 p50=680ms（三段分解：auth 160 / st 161 / challenge 160）。

### B2 扩展性 + OpenVPN 基线
| 规模 N | 准入 p50 | 吞吐 | 丢包 |
|---|---|---|---|
| 10 / 50 / 100 / 200 | ≈712ms（平稳） | 0.7 → 5.7 Mbps | 0 |

- OpenVPN 控制面握手基线 p50=110.1ms（数据面标注 simulated）。

### B3 隧道流量整形
| 业务流 | 包长熵（raw → shaped） | KL 散度 | 冗余率 |
|---|---|---|---|
| period 周期流 | 0 → 3.28 | 34.99 | 91.8% |
| request 请求流 | 1.0 → 3.56 | 33.75 | 183.9% |
| burst 突发流 | 0 → 0（1448B 无填充空间） | — | 0% |

### B4 攻击面
- 八类攻击共 601 次，全部 100% 拦截；代理伪造 5 项用例全部符合预期。

---

## 方向三：SM9 + 双 DID 的 MCP 安全访问控制

### C0 环境标识锚定
- 4 类环境（docker/linux/windows/vmware）200 个设备 DID 零冲突；克隆同标识 → 同 DID 被识别。

### C1 场景 + 审计链
- 三场景（数据查询 / 协作链 / 网关跨域）全部通过；审计链 ticket_id 贯通率 1.0。

### C2 性能（趋势性对比）
| 方案 | 并发 | p50 | qps | 报文字节 |
|---|---|---|---|---|
| 本文（SM9 双 ST + 双签名链） | 100 / 500 | 89.2s / 497.4s | 0.81 / 0.76 | 2261 / 2262 B |
| OAuth 基线 | 100 / 500 | 0.0005ms / 0.0009ms | 102643 / 30093 | 115 B |

> 说明：本文方案时延受 gmalg 纯 Python + GIL 串行限制，仅作安全性代价的趋势性对比，非绝对性能。

### C3 攻击 + 消融
- 六类攻击（tampered / priv_esc / replay / forged_st / did_spoof / confusion）：本文 100% 拦截，OAuth 与无管控 0% 拦截。
- 消融：完整方案正常 0% 误拦、攻击 100% 拦截；去掉用户签名层或 ST_data 层后，正常调用 100% 失败（证明二者为必要层）。

### C4 安全矩阵（7 维 × 4 方案）
| 维度 | Kerberos 标准 | 国密 Kerberos | OAuth 2.1 | 本文 |
|---|---|---|---|---|
| 抗重放 | 1 | 1 | 0 | 1 |
| 抗伪造 | 1 | 1 | 1 | 1 |
| 权限粒度 | 0 | 0 | 0 | 1 |
| 调用者绑定 | 0 | 0 | 0 | 1 |
| 审计归因 | 0 | 0 | 0 | 1 |
| 吊销 | 1 | 1 | 1 | 1 |
| 国密合规 | 0 | 1 | 0 | 1 |

---

## 数据文件索引

- `exp1_sm9_bio_kerberos_results/` — 方向一 A1-A5 结果 CSV（16 个文件）
- `exp2_sm9_p2pvpn_results/` — 方向二 B0-B4 结果 CSV（13 个文件）
- `exp3_sm9_mcp_results/` — 方向三 C0-C4 结果 CSV（8 个文件）

所有实验固定种子 SEED=20260817，可复现。
