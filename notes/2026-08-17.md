# 2026-08-17

## 第十八周期：方向一对比向 C1-C5 定稿

- 交付：论文大纲/方向一对比向设计-C1C5定稿.md。
- C1 自有数据算成功率（无文献数值对比）；C2 图片级加噪→重新恢复（用户口径）；C3 口令/SM2-PKINIT/本文SM9，RSA-PKINIT 数据驱动待定；C4 Kerberos 攻击面矩阵（9 类，黄金/白银票据路径，表格呈现不用圆形图）；C5 隐私论证 4 步链 + ISO 24745 四项对照表。
- C5 佐证检索命中强文献：TIFS 2021（攻击二值化+模糊承诺人脸系统，83%解锁，与本文链路同构）、Mai TPAMI 2019、Shahreza ICCV2023/FG2024、Gomez-Barrero 2020 逆向综述、Lacharme 2013 BioHashing 预映像攻击、ISO/IEC 24745:2022。

### 教训
- 隐私论证要有"对手的脆弱性证据"：传统人脸认证的脆弱性必须引模板逆向攻击实证（TPAMI/ICCV/TIFS），而非泛泛而谈；本文优势用 ISO 24745 四项要求逐一对照——论证结构 = 对手弱点（文献实证）→ 我的设计（机制）→ 标准对照（ISO 要求）。
- 检索发现 TIFS 2021 攻击对象与本文链路同构（二值化+模糊承诺）——此类"同构攻击文献"是隐私章最有力的引文，比泛综述有价值得多。

## 第十九周期：SM9 接口预留 + C1-C6 论文写作记录

- 提示词更新（5.2 节）：sm9_engine 预留方向二/三接口——verify_chain（签名链，方向三）、key_exchange 发起/响应（SM9 密钥协商，方向二/三隧道）、proxy_sign/proxy_verify（授权委托给中继，方向二）；方向一实验期间只需单测通过。
- 交付：方向一对比向记录-C1C6论文写作版.md——C1-C5 + 新增 C6（认证判定链：Rep→SM3(key_hash) 校验兜底 RS 误纠错；熔断分层恢复 L1 会话重建/L2 身份重置），论文写作可直接引用。
- 关键讨论结论（本周期）：①key_hash 校验=恢复正确性校验（hash 对象是 Rep 恢复的 bio_key'，非特征拼接）；②RS 超纠错可能误纠错到错误码字→key_hash 兜底；③σ 篡改只害自己（拒绝服务非伪造）；④熔断恢复默认走 Rep（L1），身份锚点 DID 不变。

### 教训
- 用户提出"重注册导致 key 变更"疑问揭示的架构原则：身份锚点（DID）必须与"会话级安全事件"（熔断）解耦——安全事件只清会话不清身份，否则授权链断裂。这是论文安全分析章的核心论述之一。

## 第二十周期：数据存储-流转证明-MCP兼容 + code agent 信息组织

- 交付：论文大纲/数据存储-流转证明-MCP兼容设计.md——票据存储（客户端缓存+KDC 无状态+ticket_id 字段）、流转证明（ticket_id 审计链三方案）、MCP 兼容四方案（推荐 B 头部注入+C 中间件）、MCP 调用链数据定义、论文落点。
- 提示词更新：Ticket 加 ticket_id 字段；core 加 audit_logger.py（统一审计格式，ticket_id 关联键）。
- 给 code agent 的信息组织建议：单主提示词（提示词文档）+ 3 个必要文档（需求清单相关节、C1C6 记录、数据事实）+ 默认全量文档不放行。

### 教训
- 跨方向的数据契约（ticket_id 审计链、MCP header）必须在需求阶段定义并写进提示词，否则方向二/三实现时审计不可关联。

## 第二十一周期：按 C 逻辑重构全部 md（去幻觉化）

- 18 文档 → 10 文档。删除 10 个迭代产物（方向一对比对象参数/C1C5定稿/C1C6记录/领域映射/决策落地/三方向全景/数据存储/整合分析/V6任务书/实验设计支撑——内容已被吸收或过时）。
- 新建 2 个事实源：方向一设计-C1C6.md（唯一方向一设计事实源：C1-C6 每节=实验设计+对比向+论文位置+文献）、三方向设计与论文组织.md（链路/底座/对比全景/票据存储/流转证明/MCP兼容/论文组织）。
- 提示词干净重写（不再补丁）：v6 版含约束表（gmalg 单库/ticket_id/L1L2 熔断）、11 模块、A1-A5 实验、8 项验收、预留接口单测。
- 需求清单 1.9/1.10/1.12/1.13 同步最终口径（Hamming θ 扫描/图片级噪声/C3 基线/熔断 L1/L2/新 CSV 命名）。

### 教训
- 迭代式讨论会积累大量"中间产物文档"，内容互相覆盖且口径不一致——幻觉源。重构原则：每主题单一事实源（设计/需求/代码提示词/知识库/论文草稿五类），旧文档直接删不保留。

## 第二十二周期：code agent 提示词最终版（本机地址细化）

- 提示词文档资产节更新：v4 参考代码本机路径细化到模块级（core 10 模块 + exp1-7 清单）、数据路径、产物路径 v6-大论文版/experiments/exp1_sm9_bio_kerberos/。
- 用户将用新会话（无本会话上下文）写方向一代码——提示词必须自包含。

### 教训
- 给"从零新会话"的提示词必须自包含：地址、模块清单、验收判据全在文本内，不能依赖会话记忆；本机路径写模块级（agent 可 ls 验证）比写目录级好。

## 第二十三周期：方向二学术领域对比 + 主张结构定稿

- 讨论确定方向二要素因果链：零信任环境（需求：动态授权）→ P2PVPN（载体：去中心化）→ SM9（工具：免证书国密）→ 零信任授权理念（原则：细粒度/可撤销）→ ST 票据承载授权。
- 开题对照：研究内容二 6 要素逐条对设计，发现唯一缺口 #3"节点发现与拓扑构建"；用户拍板 B 方案（协调服务注册/发现替代 DHT + 中继邻居表星型拓扑），**仅证可行**。
- 其他拍板：①中继=验证型网关（验 ST/授权后转发不碰明文，保留端到端加密，复用方向三 MCP B+C 方案）；②流量整形必做（开题承诺"带特征流量整形"，原 P2 可降级取消）；③连通率/丢包率=性能面参数与安全面分层；④D5 IPsec/TLS 基线同方向一 A4 决策（数值自实现）；⑤整形路线选"统计特征消除"（熵/KL）非"检测规则对抗"。
- 交付：论文大纲/方向二设计-D0D6.md（D0 组网基础设施+D1-D6，每主张=实验+对比向+论文位置+文献；学术领域对比表 7 行；对比分类三类；CSV 规约含 3 个新增文件）。

### 教训
- 开题承诺对照的价值：D6 整形原被标"P2 可降级"，一对照开题原文"带特征流量整形"立刻暴露是必做——需求源头（开题）优先级高于内部实验排序。
- 成熟单项技术背景下的创新定位=组合空白：学术对比每行都要写"对方单项成熟、组合空缺"，差异化不靠单项技术。

## 第二十四周期：代码设计讨论 + 部署阶段化决策

- 代码设计：复用方向一 core（did/sm9/kerberos/common/audit_logger），新增 7 模块（st_ticket/authorization/discovery/relay/device/tunnel/shaping）；对比参数分安全面（拦截率/伪造成功率/明文泄露率/密钥一致性）与性能面（发现时延/准入时延/扩展性/e2e/故障恢复/握手吞吐/熵KL/冗余率）；创新点框架=组合空白+机制+可验证实验（4 本方向+1 跨方向）。
- 用户决策：**不封装 Docker**——源码先本机多进程跑通（coordinator 用 subprocess 编排），三方向全部完成后才 Docker+Web 联调部署；"一步一步来"。已更新《方向二实验设计详解》§2 与《方向二设计-D0D6》关键决策 5。

### 教训
- 部署形态是最容易被过度设计的一环：实验价值在链路与指标，不在部署容器化；"先跑通再容器化"符合最小可验证变更原则，且让 Docker 统一复用三方向。

## 第二十五周期：代理签名定案 + 开题全文本对照

- 私钥分四层澄清（KGC主密钥/设备sk/代理密钥/会话密钥），指出"代理签名密钥分发中继"是过度设计——验证只需 KDC 公钥。
- 讨论环签名/代理签名后用户定案：**保留代理签名**，理由=P2PVPN 无法无条件打洞、KDC/中继必然存在。设计强化为 warrant 限定模型：中继真实签名任务=会话准入凭证签发（授权书限范围，不能签新授权），伪造边界=授权书范围。
- 新增 NAT 穿越条件量化（D0）：理论矩阵（RFC 4787/5389/5766）+ 虚拟 NAT 层模拟打洞 + 兜底率推导，论据链=中继必要性→代理委托有理。新增 expB1_nat.csv。
- 用户给方向二开题全文本，逐句对照 10 个描述点全闭合。

### 教训
- "给组件签名能力"与"验证"是两件事：若坚持保留签名机制，必须给它真实的签名任务（warrant 会话凭证），否则签名私钥只是攻击面——代理签名定案的关键是补上"中继能签什么"。

## 第二十六周期：方向二完整性核验 + 文档对齐

- 重温 D0D6 与详解，修正 5 处不一致：①详解 §9 "B3 可降级"→必做；②详解 §3 模块清单更新为 10 模块（authorization/discovery/tunnel/nat_layer 加入、shaping 去可选、复用方向一 core）；③D0 组网可行性+NAT 量化写入详解 §4.0；④B4 增代理伪造（warrant 越界）用例；⑤D5 基线定口径=TLS 自实现（python ssl）+IPsec 文章定性。
- 用户将据此生产方向二代码提示词。

### 教训
- 跨文档一致性核验要对照"事实源↔配套详解"两张表：决策改一处，配套文档的模块清单/优先级/基线表要同步，否则代码提示词会拿到矛盾口径（本次 B3 必做与 §9 可降级即典型）。

## 第二十七周期：方向二重构 + 代码提示词交付

- 重构：删除《方向1-2联动设计.md》（联动内容已吸收进三方向组织/C1C6 数据契约/需求清单 2.3）；其唯一独有事实"B1 绑定校验用例"迁移进 D0D6-D1 与需求清单 2.3；需求清单 §2 全面对齐（warrant 代理签名、6 类节点、8 攻击用例、整形必做、优先级 P0）。
- 交付：论文大纲/方向二实验代码实现提示词.md——对齐方向一格式（背景/复用资产/10 项技术约束/10 模块接口验收/B0-B4 实验/数据规约/10 项验收/顺序），含 §9 转交话术（自包含开场+配套参考按需给）。
- 一致性：三方向组织对比组改"SM9 vs TLS 隧道（IPsec 定性）"。

### 教训
- 转交话术=自包含开场（任务全文内嵌）+ 明确要求（复用清单/git 检查点/模拟标注/不静默跳过）+ 配套参考按需给——再次验证"从零新会话"提示词必须不依赖会话记忆。

## 第二十八周期：方向三叙事定稿 + 开题对照 + 代码提示词交付

- 叙事（从无到有）：MCP 安全无认证（实测 40% 裸奔）→ OAuth 2.1（授权态复用/粒度粗/撤销慢等 6 隐患）→ 本文 SM9 票据化；MCPS/SMCP（IETF draft + arXiv:2602.01129）为同阶段密码学化共识，作三角佐证、定位"互补非替代"。
- 外网化论证：MCP 从内网 stdio 出身→远程化强制（OAuth 2025-03）→外网化必然（网络效应/Agent 长尾能力/组织边界外数据/标准化降本/商业变现/用户即跨网 Agent）；六因支撑需求真实性。
- 开题全文本对照：方向三 14 个承诺点全部闭合（无缺口）；2 处措辞对齐（Agent=特殊设备节点、中继验证写全 SM9 授权+ST+DID）。
- Agent 身份锚点可行性：docker container id / linux machine-id / windows MachineGuid / vmware UUID 四类唯一数值 → make_device_did 派生，环境重建/克隆由重注册+登记表兜住。
- 代码设计：复用方向一 core（did/sm9_engine/kerberos_enhanced/audit_logger），新增 8 模块（st_ticket/mcp_protocol/mcp_agent/claims_checker/mcp_server/mcp_gateway/oauth_baseline/noauth_baseline）；三方案对比（无管控/OAuth/本文）+消融（去用户签名/去 ST_data）；C3-6 调用者混淆攻击复刻 OAuth 授权态缓存缺陷；实验 C0 可行性→C1 场景→C3 攻击→C2 性能→C4 矩阵。
- 交付：论文大纲/方向三实验代码实现提示词.md（对齐方向二格式，含 §9 转交话术）。

### 教训
- "从无到有"叙事要有基线分层：无管控=内网遗留时代、OAuth=远程化标配时代、本文=票据化——每个基线代表一个真实历史阶段，对比才立得住；OAuth 基线的缺陷要"忠实复刻"（授权态缓存复用）让攻击真打穿，而非只引文献定性。
- 同阶段工作（MCPS/SMCP）对学位论文不是威胁而是佐证：定位为"共识的确认+路线的差异化（国密/票据/计量）"，避免与首创之争纠缠。
- 环境唯一标识（docker/linux/win/vmware）把"Agent=特殊设备节点"从假设变成工程事实——可行性论证要找"每类部署形态都存在的唯一数值"，而非泛泛说"有唯一标识"。

## 第二十九周期：两本汇总版定稿（单一事实源收敛）

- 用户确认：三方向设计已全部确认完毕，要求产出"论文方向"与"工程实践方向"两本自含总文档；旧分方向文档吸收后删除。
- 交付：
  - 论文大纲/方向设计汇总版.md（论文写作事实源）：吸收原《方向一设计-C1C6》《方向二设计-D0D6》《三方向设计与论文组织》——三方向链路/论文组织/C1-C6/D0-D6/C0-C4 叙事+开题对照/专利定稿口径/票据流转/MCP 兼容/文献汇总。
  - 论文大纲/代码汇总版.md（工程实施事实源）：吸收原《方向一/二/三实验代码实现提示词》《方向二实验设计详解》——§2 全局规约（专利常量总表+指标定义总表+CSV 规约+跨方向数据契约）、§3/§4/§5 三方向完整规格（数据口径/常量/模块接口/实验/验收）、§6 CSV 全索引（28 文件→论文落点）、§8 转交话术。
- 删除 7 个分方向文档；需求清单 1.9 引用改指汇总版。最终论文大纲 = 7 文件（2 汇总版 + 需求清单 + 专利对照 + 综述 + 框架/完整版）。
- 关键设计：指标定义总表（KRR/FAR_key/AUC/EER/拦截率/熵/KL/冗余率/兜底率/审计贯通率等 30 项，每项含公式与来源 CSV）——这是"论文直接取数"的机制：论文引用数值时必须能在 §6 索引找到对应 CSV。

### 教训
- 汇总版的价值不在合并文字，而在补"跨方向公共层"：常量总表、指标定义、CSV 全索引这三件套是分方向提示词没有的——它们让三方向的数据契约（ticket_id/审计链/复用接口）与论文取数路径变成显式事实，而非散落各文档的隐式约定。
- 汇总时发现的分方向口径冲突要显式落表（如专利 SHA-256/BLAKE2b vs 实验侧 SM3），写"专利→实验映射"而不是悄悄二选一——消歧即防幻觉。
